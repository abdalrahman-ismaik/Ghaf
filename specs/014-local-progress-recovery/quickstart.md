# Proposed recovery verification quickstart

DRAFT; none of these new-feature checks has run. Obtain the accepted contract and file grants first.

1. Run the current existing task with synthetic Salem and record each independently owned baseline.
2. Restart at assignment, choice, in-progress/help, submission and confirmed-pending-recognition.
   Re-enter the correct role; verify no stored assistant/media and no early award.
3. Present praise and recognize once. Reopen/restart repeatedly; verify all FR-008 results remain
   unchanged. Duplicate and out-of-order commands must not add +12 or replay celebration.
4. Inject repository read/write errors and corrupt/unknown/oversized payloads. Verify last saved
   state or blocked recovery, never partially restored defaults or a silent successful-save claim.
5. Recognize then replace with reused IDs/identifier/timestamps; retry recognition in the new family.
   Inject failure at every final-save/cleanup/rollback boundary; old or committed-new, never mixed.
6. Reset before/after every meaningful state and while prepared callbacks are pending. Restart and
   repeat reset; signed-out Arabic baseline must remain, with no retired-generation revival.
7. Run proportional automated checks and full integrated suite once. D retests exact candidate.
8. Build and verify a standalone APK, then run native process-death, SQLite interruption, offline
   cold launch, Back/IME/TalkBack/text-scale/reset on both actual phones independently. Record
   missing devices and human review as BLOCKED/NOT RUN; browser screenshots are separate evidence.

No memory, broader coach, network, flag activation or production account is part of this quickstart.
