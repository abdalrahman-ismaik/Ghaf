# Design-Intake Decision Log — Ghaf R001 Batch 1

**Date:** 2026-09-02

| ID   | Decision                                                                                                                                                                         | Rationale and boundary                                                                                                                   |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| D001 | Record `ghaf-r001` as user-approved only for Welcome and first-time Parent onboarding.                                                                                           | The user explicitly named this first approved batch; no later screen was supplied or authorized.                                         |
| D002 | Use each PNG as canonical composition and each HTML file only as a non-runtime hint.                                                                                             | Matches the user's authority rule and prevents DOM/Tailwind/generated-JS import.                                                         |
| D003 | Treat the seven 487 px-wide, variable-height PNGs as proportional references for 390×844 review.                                                                                 | A fixed 390×844 or 487 px canvas would break safe areas, keyboard, smaller devices, and font scaling.                                    |
| D004 | Record missing `screen-spec.md`, English frames, and state frames rather than blocking this requested batch.                                                                     | The user instructed implementation to continue and explicitly required native behaviors; variants receive no fabricated visual approval. |
| D005 | Keep the Welcome hero from the PNG and add Parent/Child actions in its scroll continuation.                                                                                      | The PNG crop omits them, but product requirements and HTML both require the actions.                                                     |
| D006 | Keep Child entry unavailable in this batch and never route to `/role`.                                                                                                           | Child access frames are missing and the historical role toggle is prohibited.                                                            |
| D007 | Use deterministic local code `424242` and the synthetic phone example ending in `42`.                                                                                            | Enables verification, error, loading, success, and offline demonstration without SMS/email or production-auth claims.                    |
| D008 | Keep `عائلة النخلة` as a screen-local onboarding draft; do not rewrite the canonical Al Noor later-journey fixture.                                                              | The approved frame and active demonstration oracle differ. The full domain migration remains blocked.                                    |
| D009 | Carry the chosen botanical avatar and optional supports into Review.                                                                                                             | The Add Child and Review exports disagree; state continuity is more truthful than reproducing a static mismatch.                         |
| D010 | Allow Arabic, English, or both only as the Child's preference; show one app locale at a time.                                                                                    | Preserves the visible control without violating localized-screen requirements.                                                           |
| D011 | Let the root product type minima and 48dp rule override smaller generated labels/chips.                                                                                          | Child/elder access, Arabic readability, and the approved accessibility contract outrank generator defects.                               |
| D012 | Implement one hidden/native OTP input with responsive visual cells.                                                                                                              | Preserves the six-cell composition while supporting paste, deletion, accessibility, bidi, and 320 px widths.                             |
| D013 | Implement success as a native transparent modal route over Review.                                                                                                               | Reproduces the canonical composition, preserves Back/dismiss semantics, and makes navigation state explicit.                             |
| D014 | Use code-native SVG and tonal opacity instead of Material Symbols font, CSS blur, or generated assets.                                                                           | Existing native architecture already supports SVG; release asset directories are empty.                                                  |
| D015 | Bundle `@expo-google-fonts/alexandria` 0.4.2 at 400/700/800, `@expo-google-fonts/readex-pro` 0.4.1 at 400/500/600/700, and compatible `expo-font`; make no runtime font request. | These are the integrated roles/weights. Loading and error fallbacks remain deterministic and offline.                                    |
| D016 | Use `/parent` only as the success integration destination; do not redesign Parent Home.                                                                                          | The success interaction needs a destination, but no Parent Home frame is approved in this release.                                       |
| D017 | Implement focused, disabled, loading, validation, offline, and success variants from shared semantic states.                                                                     | The user requires these behaviors; the lack of frame variants is recorded as a visual-evidence gap.                                      |
| D018 | Keep only explicit header/action/sheet regions anchored; let central content scroll.                                                                                             | Matches the supplied compositions while protecting safe areas, keyboard use, small screens, and 200% type.                               |

No open P0 product or safety conflict remains inside Batch 1. Missing later screens, English/state
references, physical Android evidence, and the full Al Noor migration remain explicit blockers for
the complete Revision 2 release.
