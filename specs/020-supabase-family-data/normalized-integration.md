# Preserved normalized runtime integration

Authority: the user's September 15 request to add the approved Feature 019 branch to `main`.
Feature 020 remains the deployed default. Feature 019's separately verified normalized runtime
is retained under explicit build selection while its hosted installation remains unverified.

The integration must preserve both model/API/resource contracts without combining incompatible
DTOs, interpreting missing RPCs as a runtime switch or assigning fixture identities to real data.
Account credentials and Child enrollment remain bound to the selected project and runtime.
Existing shared account profiles and legacy planning records remain preserved; no cross-runtime
family/progress transfer is inferred. All source and migration history remains available.

Acceptance: default Feature 020 dispatch and explicit normalized dispatch, separate translations
and services, no Child-to-Parent authority bleed, existing task/form/language fixes, UAE Masroofi
sample/runtime preservation, passing integration regressions and truthful deployment evidence.
