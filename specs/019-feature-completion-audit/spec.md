# Feature 019 — Requirement audit and completion

Authority: the user's 2026-09-14 R01–R38/A01–A07 audit-then-implementation instruction. It authorizes correcting existing functionality and implementing feasible missing behavior. This additive contract records the first concrete slices before code changes; the ledger retains all other requirements.

## Current accepted slices

1. R03: A private family memory timeline. After a Parent-recognized eligible household task, the Parent may explicitly save one text memory from that authoritative completion. Eligibility uses household visibility and excludes sensitive/private categories; first increment accepts Green Impact activity only. No media capture, assistant text, freeform Child content or fabricated relative is imported. Each memory keeps its family-instance identity, completion/occurrence ID, Child identity, bilingual task title and confirmation timestamp; duplicate retries return the existing memory. Saving/deleting memory never changes task/Seed/League/reward evidence. Parent-only deletion is prospective presentation deletion, with a tombstone to prevent automatic recreation. A Child may view only their eligible memories, Parent may browse this household. UI is reachable from Garden, uses current guards/tokens/RTL, reports local-only versus quick-demo session-only and storage failure honestly. Ordinary local-family memory storage survives restart; quick demo/pilot sample remains isolated memory. Reset/replacement cannot expose prior household records, including reused fixture IDs. No cloud memory synchronization is claimed.
2. R09: Optional academic goal target and review dates using YYYY-MM-DD civil dates (not UTC instants). Validate actual calendar dates and review >= target when both exist. Legacy saved goals without dates load with null dates. Dates are jointly agreed terms, so existing accepted-goal edit/approval and immutable unlocked-prize protections apply. No overdue punishment, notifications or Google Calendar claim.
3. R30: Preserve original six-screen onboarding, exact approved art/copy/narration. Add persistent completion for ordinary installation flow; quick demo remains explicitly repeatable. Marker has no authority, role, family or private data. Explicit reset may clear/restart onboarding; unreadable/corrupt marker shows onboarding rather than bypassing it. Playback provides a working stop control and text equivalent with normal background/focus/reduced-motion cleanup.
4. R19: Repair saved-template identifier reuse after deletion/retry without changing wording/policy/award authority, stored content or repository schema.

5. R02: Explain existing prepared category recommendations using allowlisted reason codes derived from the same authorized preference matches. Do not add ranking rules, repeat raw custom descriptions, claim a live model call or bypass opt-out. Profile preview and Task Builder show the concise localized explanation.
6. R31: Extend the existing ambience preference with an adjustable, bounded quiet volume and backward-compatible defaults for previous boolean-only records. Persist through the same repository; failed writes preserve previous audible preference, invalid/unreadable data remains silent. One root player uses the selected level and existing narration ducking, screen-reader, foreground and recording exclusions. Settings uses accessible finite choices and existing mute control; no audio package or second player.

7. R22/R36: Add an opt-in server-only Gemini text provider to existing bounded Worker operations, selected only by server configuration. Preserve default Workers AI behavior and all existing authentication, schemas, age/privacy policy, output validation, limits, fallbacks and disabled release state. Use official REST generateContent with explicit server model and secret key, fixed Google HTTPS origin, bounded response size, cancellation and content-free failures; no client secret, runtime MCP requirement, tool execution, media upload or automatic provider fallback. Fake-network tests prove adapter wiring only. Trusted broker/deployment/real-service validation remains explicitly blocked; no flags are enabled by this slice.

8. R20: Allow an authenticated local Parent to edit the existing primary/optional second guardian and up to six optional family connections after creation, using the same validated fields, editor and prepared recognition-only suggestions. Preserve family identity, Child profiles and all task/award evidence. Cancel writes nothing; stale family or failed/ignored storage writes cannot report success. A Parent settings entry reaches the editor even when no relatives exist. Children cannot mutate or see the private directory. This edits local synthetic family data only; it does not create contacts, invitations, location, AI inputs or cloud relatives.

## Boundaries and conflicts

Historical Feature014 progress recovery was expressly deferred and is incompatible with the expanded 24-task runtime. Current R01/R18/A01 ask for persistence and remain open requirements, but this increment does not pretend a memory receipt is recovered reward authority. A later complete recovery implementation needs validated full current state, separate service ledgers and failure-atomic reset. Feature016 messaging and Feature018 account workspaces keep their own authorities; no synthetic-to-real upload or task sync is inferred.

Gemini/Firebase user diagram is intended architecture evidence; existing Supabase/Cloudflare code is implementation evidence. Replacing working identity/storage or activating unsafe Child AI is not inferred. Record exact broker/configuration/provider blockers and implement useful independent slices.

All code retains default-off R002b flags, Parent approval, private rewards, Child dignity, local-demo/real-account separation and prior motion. No production data mutation, payment, invitation, live call or child media is authorized for verification.

## Acceptance

Focused real-domain/repository tests must prove restart readback, duplicate prevention, wrong-family/wrong-Child/Child-mutation denial, storage failure and reset isolation for memories; legacy goal migration/date validity/accepted-term protection; onboarding remount/completion/stop/reset behavior; saved-template delete-then-save uniqueness. Full type/lint/format/tests are run once after integration. Device checks remain separately stated. The implementation ledger is the acceptance inventory and records incomplete work honestly.
