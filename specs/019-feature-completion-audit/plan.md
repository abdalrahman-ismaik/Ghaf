# Implementation plan

Reuse current Expo 57 / RN 0.86 / Zustand / Zod / SQLite/localStorage / bilingual botanical UI. No dependency upgrade or parallel task/calendar/auth backend. Root integrates one serialized check lane and source-aware delivery records. Four helpers own disjoint slices, preserving NAV-MOTION reservations. A new memory feature may use the established local repository and family-instance identity pattern; it cannot hydrate or authorize rewards from memory data. Goal dates extend existing study schema compatibly; onboarding persistence stores a completion-only marker; template ID fix preserves its existing schema.

Run focused regression checks for each slice; root performs aggregate checks. No local Gradle/export while disk remains constrained. Review historical configured-service evidence without representing it as current source execution.

