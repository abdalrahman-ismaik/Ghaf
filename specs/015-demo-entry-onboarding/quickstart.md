# Feature015 development and verification

Implementation is gated by accepted contract commit and exact board grants; commands below describe
candidate operation, not a claim those features/builds already exist.

- Ordinary regression run: `npm start` with EXPO_PUBLIC_GHAF_DEMO_ENTRY unset.
- New demo run after integration: `EXPO_PUBLIC_GHAF_DEMO_ENTRY=true npm start -- --localhost`.
- Existing terminal preview was paused by explicit user approval; A must restore it in demo mode
  after B native-heavy work releases resources and the015 source is ready. Do not start a second Metro.
- For APK, B uses its private dependencies/pinned toolchain and A's exact source; set the same explicit
  demo flag before JS bundling and record it in the artifact receipt. No cloud build/public release.

Check each profile from signed-out entry, one active role, invalid/deep-link/other-sibling denial;
perform Parent→Salem→Parent +12 with help; sign out without reseeding; reset to Arabic exact defaults;
restart to fresh signed-out demo; verify ordinary stored family bytes are unchanged. Use real store/
controller actions in automated tests, then D independently checks the exact APK on actual devices.

Explore three optional story moments in AR/EN, skip/replay and large text, with every medium absent.
No narration is considered repaired until an Arabic reviewer listens to the exact matching clips.
Voice/native/student results remain separate. Recovery014, other features and R002b activation stay off.
