# Main integration — 2026-09-11

User request: merge the completed integration work into `main` and continue working there.
Integration owner: `/root`. This is a local merge; no remote publication is included.

## Inputs and preservation

- Previous local `main`: `e155201`, containing fetched `origin/main` plus one local commit.
- Completed implementation: `b221284`, followed by ownership reservation `4692c28` on
  `integration/r3-complete-screens-20260905`.
- Common ancestor: `a6ca21a`. The initial comparison found 28 main-only and 157
  integration-only commits; the subsequent ownership reservation adds one integration commit.
- Normal three-way preflight found 29 conflicting files. The merge preserves both histories;
  no reset, rebase, force-push, or branch deletion is used.

Main's unique product, design, runbook, ownership, and Feature 003 evidence changes are retained
verbatim as a Git diff from `a6ca21a` to `e155201` in
[the compressed historical evidence patch](main-r3-20260911-history.patch.gz). Gzip preserves
the original patch bytes, including context whitespace; decompression was verified by SHA-256.
The patch is an archive, not
the current product contract, an implementation instruction, or a new acceptance result.
Original files remain recoverable from `e155201` in the preserved merge ancestry.

All five main-only screenshots remain at their existing paths under
`output/playwright/family-experience/` and `output/playwright/feature003-voice/`.
Their old evidence does not pass current physical Android or named-human gates.

## Reconciliation decisions

The current approved R001/R002a/R002b and Features 004/005 implementation is the runtime and
active-document baseline. Main's older Family Experience controller and three panels use a
direct role switch, eager synthetic sessions, different reward progress, and a second set of
League/Reward authority. Those files and their obsolete presentation/store tests are superseded
by the current isolated access controllers, dedicated routes, and deterministic authorities.
They remain in Git history; they are not reconnected to the new store.

The merge keeps the current coherent RTL, typography, startup, audio, reset/navigation,
dependency/lockfile, and default-off AI implementation. Main's alternative root-direction
strategy is not mixed with the newer logical-layout helpers. No AI or Growth flag is enabled.

Compatible main improvements carried forward:

- Android `allowBackup: false`, keyboard `resize`, and the two external-storage permission
  blocks. Current foreground audio permissions/plugins remain; background recording and
  playback remain disabled.
- Reviewed 124,028-byte recycling WebP and provenance, replacing the 2,268,093-byte PNG only
  in the runtime import. The original PNG remains archival and the newer startup preload
  export is preserved.
- Header action wrapping, bounded action width, and an explicit heading role, while retaining
  current RTL direction and typography.
- The prepared-image payload/metadata tests. Android readiness tests are reconciled with the
  current architecture rather than requiring ten routes or forbidding approved audio.

## Validation and handoff

The merged checkout is on `main`. The reconciled Android-readiness tests passed 5/5 cases.
Expo public configuration confirms backup disabled, keyboard resize, external-storage blocks,
and the approved audio permissions. Web export passed with 39 routes; Android JavaScript
export passed with one Hermes bundle and 94 assets, including the smaller recycling WebP.
Web retains the known file-system and color-environment warnings.

The online `npx expo install --check` reported newer SDK 57 patch recommendations for 13
packages. This dependency freshness check did not pass; the merge keeps the tested package and
lockfile versions rather than introducing a dependency upgrade. The offline check against the
installed SDK matrix reports dependencies up to date but also warns that offline validation
is unreliable; it is not presented as an online compatibility pass.

Final validation passed:

- `npm.cmd run typecheck`
- `npm.cmd run lint` with zero warnings
- `npm.cmd run format:check`, plus scoped documentation/asset-provenance formatting
- `npm.cmd test -- --maxWorkers=2`: 125 files / 1,552 tests
- Strengthened each forbidden metadata-chunk assertion in the inherited asset test; its final
  focused rerun passed 3/3 cases and formatting passed
- `npm.cmd run build:web`: 39 static routes
- `npx.cmd expo export --platform android --output-dir .expo/main-merge-android`: one Hermes
  bundle and 94 assets
- Working/index whitespace checks, no unmerged index entries, and a bounded independent diff
  review

All ownership reservations are released. The local merge is ready and `main` is the working
branch for subsequent work. Both source histories are preserved; no push or release activation
occurred. Native backup/keyboard/media behavior and named-human reviews still need direct
evidence; configuration and JavaScript exports cannot pass those gates.
