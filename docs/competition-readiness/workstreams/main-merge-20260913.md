# Main reconciliation — 2026-09-13

## Objective and authority

The user requested: "push everything and merge, solve any conflict". The worktree
was already merging local `9fa5aee` with `origin/main` at `abc8598`, with 25
unmerged paths. The fetched remote main and UI publication branch both point to
`abc8598`. Root completes this existing merge and a normal push to main.

## Resolution

- Preserve the adult-only pilot gate and memory isolation alongside the incoming
  demo entry, catalog, saved templates, shared ambience and messaging boundaries.
- Preserve verified corrupt-family recovery, schema migration/profile repair and
  all reset authorities. Repairable legacy profiles must not be cleared as corrupt.
- Combine startup/hydration readiness with the Tamagui shell, shared audio and
  onboarding. Carry the screen-reader race safeguard into the shared audio owner.
- Use the current prepared navigation reset before confirmed local recovery.
- Retain Supabase and Tamagui dependencies in the combined manifests and lockfile.
- Preserve bilingual resources, both sets of product/audit evidence and the
  incoming documentation/test relocation. Move local regression additions into
  their subject directories and record their paths in `tests/legacy-paths.json`.
- Preserve the 30 audit screenshots already committed on the local parent by
  adding their exact paths to the incoming historical-artifact manifest. The
  policy still rejects new generated output outside its curated locations.
- Keep repairable legacy profiles on the existing Parent repair path instead of
  trapping them on the corruption retry screen; cover both cases at Welcome.
- Allow measured cold initialization in the existing Expo configuration and ESLint
  integration tests: 15 seconds for Expo and 45 seconds for the first ESLint probe.
  Repeated full runs measured approximately 5–10 and 31–35 seconds respectively.
  Their assertions and all other test timeout limits remain unchanged.

## Authorship and file ownership

Codex performed this merge with three bounded helpers for core services/store,
entry/onboarding UI and Markdown reconciliation. Root owned dependency installation,
regression integration, validation and Git publication. The repository's configured
contributor identity remains unchanged; a commit identity is not proof of student
authorship. Exact write grants are in the current Team Ownership entry.

## Validation

Checks completed against the combined source:

- `PASSED`: combined-lockfile `npm ci`, TypeScript, zero-warning lint, formatting
  and repository navigation/test-path/historical-artifact checks.
- `PASSED`: final full suite, 182 test files and 2,623 tests; one opt-in local
  Supabase integration test is `NOT RUN` because its local service was not enabled.
- `PASSED` with limitation: Expo's offline dependency check reports dependencies
  up to date; the CLI notes offline validation is unreliable and does not replace
  online registry verification.
- `PASSED`: independent source review of pilot gating, isolated storage, demo
  entry and reset/recovery after correcting the repairable-profile Welcome trap.
- `PASSED`: publication review of 640 changed tracked text files found no material
  credential/private-input issue; README references and legacy test paths resolve.

- `PASSED`: default web static export, all 40 routes. Expo returned success after
  its existing forced-shutdown warning; output is present in the scoped directory.
- `PASSED`: Android export, one 8.8 MB Hermes bundle. The first sandboxed compiler
  execution was denied; the approved compiler retry completed successfully.

The source boundary is ready for integration and the user-authorized normal main
push. Both parents and all resolved paths are preserved in the merge commit.
Logs are local ignored artifacts under `.expo/merge-20260913/`.
No hosted provider, physical Android, new browser rehearsal or named-human
acceptance is established by this source merge. Existing release gates remain.
