# Parent dashboard — Dailoz reference adaptation

Session C, `C-20260912T011718Z-root`; 2026-09-12. User selected Dailoz and then
explicitly authorized the concrete four-file implementation with “go ahead”. A228,
board revision 77, acknowledges these exact paths. This is applied presentation work
inside Feature 003's existing behavior. Human exact-diff acceptance is pending.

## Visible problem and resulting action

The old dashboard put a large cooperative canopy before the current task. The Parent's
next responsibility fell below the first viewport. The updated reading order is welcome,
current task or matching adjustment, Child selection, smaller canopy, existing summary
and privacy context. The pending Arabic action is «مراجعة الآن»; English is “Review now”.
Both keep the complete permitted-help statement and the displayed 12 Seeds **after Parent
confirmation**. Waiting does not display newly confirmed growth.

The user-selected `dailoz-app-vektora-studio` Home artboard
`0512C301-0E46-4CEB-958B-F29FD231AEC1` and Task/empty Task
`0A4E1BC9-5C3D-484A-9674-5C079ED57FF3` informed section rhythm, compact task panels,
status hierarchy and one clear next action. The existing template catalog and
`output/competition-readiness/sketch-inspection-20260912-c/` retain archive and layer
provenance. No imported code, image, font or dependency enters runtime. Dailoz's purple
palette, fabricated productivity statistics, calendar controls and pressure language were
rejected. Ghaf retains its official mark, botanical artwork, Alexandria and Readex Pro.

## Exact implementation boundary

| File                                                      | Change                                                                                                                                                                                      |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/parent/index.tsx`                                    | Presentation only: action/adjustment before Child section and canopy; smaller welcome typography and explicit direction. Commands, queries, guards, routes, conditions and flags unchanged. |
| `src/components/r002a/parent/ParentLifecycleCard.tsx`     | One paper task surface, consistent inset, readable help, explicit direction and test identifier. All existing metadata/action props preserved.                                              |
| `src/components/r002a/parent/ParentChildrenSection.tsx`   | Compact 48dp avatars, selectable rows, visible check plus Selected wording. Full next-step/support text retained. Explicit heading semantics preserved.                                     |
| `src/components/r002a/parent/ParentCanopySummaryCard.tsx` | Smaller image and calmer paper surface; full cooperative meaning, numeric progress and existing display ratio preserved.                                                                    |
| `tests/parent-dashboard-presentation.test.tsx`            | Fourteen bilingual rendered callback regressions using actual route/components and store commands.                                                                                          |

The four-source diff is 79 insertions and 48 deletions. Parent Tasks/navigation is a
separate proposed slice, not implemented by this handoff. Shared tokens, translations,
store/session/award authority, Child screens and A's entry changes are untouched.
Home retains its global household journey: selecting Alya does not relabel Salem's task.
The existing create action still explicitly selects and names Salem. Local family names
remain readable mixed Arabic/English text; they are not translated by the screen.

## Candidate identity and evidence

Shared root `/home/smyk/projects/Ghaf`, branch `redesign/ui-experiments`. Applied at
`6b8d4bb4633f1615fdf5dbf1dba9845d71aa56a8`; A concurrently committed disjoint entry work
as `82c8647ad4ed8c9570a0efb5d651c025cd646d30`. C source is bound by SHA-256 below,
not by an assertion that the entire concurrent application was immutable.

| Source       | SHA-256                                                            |
| ------------ | ------------------------------------------------------------------ |
| Parent route | `a3bc5ba77f2f1754177e3c02c2a4ac3d9d013501f9224a53ad5072a8ac7deb38` |
| Lifecycle    | `8b833e7a20d100c4ae45aac01059a438406b77e569e375a5de072bfade2fe36e` |
| Children     | `a58927b3bf1008b31288eb77dd11718f73cd35ea2a13d684f4cfc3a13920920b` |
| Canopy       | `ac692ea2aceb248cca4239682de45840648727f0dff7ae0c20be796d0ff7d785` |

Packet: `output/native-ui/parent-dailoz-20260912/`. `index.html` presents paired Arabic
and English before/after captures. Before images are byte-identical historical poster
captures with their original pixel resolution; `before-provenance.json` records origins
and hashes. They are not fresh same-run before captures. `applied-source.patch`,
`applied-source/`, `applied-validation.json`, raw browser tool receipts and source-map
proof make this slice inspectable. Earlier `brief.md`/draft receipts remain explicitly
historical, unapplied draft records; this report supersedes their status.

Existing Metro PID366844/8081 was verified at the canonical root. Its source map at
16:42:57 UTC contains all four exact disk sources. Sole C Firefox PID408291 used private
profile `/tmp/playwright_firefoxdev_profile-0wsv1l`; no new Metro/cache reset was performed.
Browser closed and PID absence verified at 16:49:27 UTC. Metro remains for A/user preview.

## Validation and limits

| Evidence                                          | Result                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Focused rendered tests                            | **PASSED 14/14**, 1.83s, Vitest4.1.11, exit0. AR/EN submitted/retry/confirmed action routes without growth; Child selection/global journey; explicit Salem creation; Tasks filters/recovery; matching safe-equivalent adjustment handoff; wrong-role exclusion.                                                 |
| Scoped Prettier / ESLint / whitespace             | **PASSED**, all five source/test files, exit0.                                                                                                                                                                                                                                                                  |
| Impeccable layout/type source detector            | **PASSED**, no findings. Mechanical evidence alone does not establish design quality.                                                                                                                                                                                                                           |
| Ready and submitted, AR/EN, 320/390 × 844 CSS px  | **PASSED bounded browser layout**. Complete primary label/help, no document horizontal overflow. Ready CTA height58/60; pending58/60 and visible above bottom navigation. Representative Arabic/English compact images visually inspected.                                                                      |
| Confirmed AR/EN390                                | Correct “continue praise/results” action captured; no newly awarded growth from this visual probe.                                                                                                                                                                                                              |
| Selected Alya, EN390                              | Real focus/Enter changes selected Child; visible label/check. `aria-selected` was absent from web button output, so no ARIA-selected pass is claimed. Native selected state unchanged; TalkBack unrun.                                                                                                          |
| Canopy EN390                                      | Complete progress/remaining/cooperative meaning visible, image crop inspected. Display stays19/25 and says no Child comparison.                                                                                                                                                                                 |
| Corrected CSS200%, reduced motion, AR/EN320       | Full HTML leaf text doubled, including spans. Action text16→32px; resulting button86px Arabic/82px English, complete readable labels, no horizontal overflow. Content remains scrollable. Existing shared bottom navigation wraps words under stress; this is not a whole-screen200% acceptance.                |
| Route/Back under partial visual fixture           | **NOT PASSED**: navigation wait timed out. Only journey/assignment fields had been installed, not full recognition-admission ledger. Check-in resolves admission and redirects on failure. This probe cannot establish an end-to-end regression or pass. Real callback destination is covered by focused tests. |
| Image failure, full long custom nickname matrix   | **NOT RUN** in this bounded pass; unchanged existing fallback/copy interfaces.                                                                                                                                                                                                                                  |
| Integrated typecheck/full suite                   | A's serialized candidate check pending; earlier app passes are not inherited.                                                                                                                                                                                                                                   |
| APK/device, native Back/touch/TalkBack/font scale | **NOT RUN / hardware acceptance pending**. CSS widths and doubling are not device dimensions or native font scale.                                                                                                                                                                                              |

Browser visual fixtures intentionally set only journey/assignment and locale fields using
existing local fixture exports; they do not grant awards, change session authority, or
establish the full family journey. Seeds48 and canopy19 stayed unchanged in recorded rows.
The final failed route probe stopped before fixture restoration; closing its isolated
browser discarded that context. No claim is made that it completed the planned Back check.

Preserved harness failures: the first AR390 image contained the transition overlay and
is invalid; settled `ready-ar-390.png` corrects it. Initial stress images selected only
leaf divs and missed spans; they are partial probes. `corrected-stress-*` and the raw
font-size receipt supersede them. Initial test CLI rejected unsupported `--minWorkers`;
the first valid run passed12/14 with two English fixture setup errors. Setting locale
after the access helper fixed those tests. These are not manufactured product RED tests.

Commands: `NODE_OPTIONS=--max-old-space-size=256 timeout 60s node node_modules/vitest/vitest.mjs
run tests/parent-dashboard-presentation.test.tsx --pool=threads --maxWorkers=1`;
local Prettier `--check`, ESLint and `git diff --check` on exactly the five files;
`node .agents/skills/impeccable/scripts/detect.mjs --scope layout,type --json` on four sources.
Runner413166/session3431 exited; prelaunch memory35.45% available. No full suite/native job.

## Student explanation and human review

Impeccable layout/Operate and Expo design-system skills guided the lead's bounded
composition changes. Playwright supplied browser evidence. Lead authored the four-file
diff, inspected captures/root proof and reviewed the helper test. Helper
`/root/parent_dashboard_state_review` mapped states, reviewed the candidate and authored
only the new regression test. It identified and corrected the draft's lost heading role;
the lead retained an explicit accessibility heading. It has no live jobs/descendants and
has released its file. Actual prompts, contributions, rejected ideas and command outcomes
are retained in the packet's assistance records and this report.

Requested settings: GPT-6 Astra, Ultra, Fast. Helper launch explicitly selected
`gpt-6-astra`/`ultra`; effective serving/Fast selector is unexposed. No root settings
change or provider capability is invented. No generated runtime artwork or external TTS
work occurred in this slice.

User selected the reference and implementation. Named student owner/exact-diff review,
human design acceptance, native accessibility and complete primary-phone rehearsal remain
**PENDING**. A student can explain: “We moved the Parent's next decision before the garden,
kept support and the full award visible, and made profile selection explicit. Tests check
that changing presentation neither changes authority nor grants progress.” No interview,
student participation or acceptance has been fabricated.

## Integration release and recovery

Four source files, focused test, this report and completed ignored packet are released
to A for the serialized shared-index commit and integrated checks. C does not stage live
coordination or concurrent A/B/D files. C retains only its STATUS writer after release.
No browser/helper/heavy/native job remains. No push, merge, deployment or release flag.

D should assess the integrated candidate independently. On the primary Android, enter
the synthetic Parent, inspect Home in AR/EN ordinary/enlarged text, verify complete pending
label/help, Child selection reading order, 48dp targets, TalkBack announcement and Back;
check the same final content with reduced motion. Collect device/model/OS/font settings,
installed candidate hash and screenshots only when observed. Parent Tasks/navigation
continues only on its own exact grant; no eligible next source grant is currently recorded.
