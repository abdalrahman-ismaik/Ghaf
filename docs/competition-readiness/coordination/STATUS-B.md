# Session B status — Core journey and recovery

**Sole live writer: Session B lead.** Canonical live file; A alone stages this record.

## Current snapshot

- State: all granted B work COMPLETE/RELEASED; PAUSED for A coordination checkpoint, then resumable handoff pending next eligible grant.
- Instance: `B-20260911T2217Z-3572e5c9`; human owner/reviewer PENDING, unknown.
- Last actual update: 2026-09-11 22:36:43 UTC.
- Worktree `/home/smyk/projects/Ghaf-demo-systems`; branch `redesign/demo-systems`; HEAD `a081f642a3241c6d6965da67de4f3438245c7c3f`; patch parent `b1fc581`; initial source baseline `02b9618`.
- Board revision 7 ACTIVE read; A ACKed/integrated B-009 and received B-010/011. B-004 uses existing Feature 011 at `02b9618`. Recovery 014 remains DRAFT/no code grant; A helper owns remaining restoration-feasibility analysis.
- Held paths: canonical STATUS-B only. Explicitly RELEASED to A: entire `src/state/usePrototypeStore.ts`, `tests/family-replacement-flow.test.ts`, and `docs/competition-readiness/workstreams/b-recovery-audit.md` at `a081f642a3241c6d6965da67de4f3438245c7c3f`. No source or report write boundary remains held.
- Dirty tracked/untracked paths: none (`git status --short` empty after commit). A-provided dependency symlink and Expo env ignored/read-only; no package/bootstrap change by B.
- B helper quota 1 unused/released: read-only 014 evidence mapping complete; no live helpers, descendants, jobs, heavy or preview allocation.
- Current action: ACK A-013 status checkpoint; STOP status writes until A RESUME. B source/audit work is complete; next B runtime work requires an accepted committed story + exact grant or newly assigned reproduced defect. A/D continue integration/review; no B producer is pending.
- Settings: local user config `gpt-6-astra`, reasoning `xhigh`, tier `fast`; effective root reasoning/tier unverified, no Ultra claim. Helper explicitly requested Astra/ultra; Fast not exposed. No config change.
- Evidence: 8 relevant files/132 tests pass; final 10-case replacement rerun passes with exact Reward delta; plain typecheck now passes after A's r5 bootstrap, scoped lint/format pass. Browser/native NOT RUN, student PENDING.

## Helpers and local jobs

**2026-09-11 22:35:13 UTC**: `/root/replacement_audit` finished its narrowed evidence-validator map, reviewed by B; RELEASED. No writes, tests, jobs, browser/native execution or descendants. Lead checked the positive-award versus duplicate-validator distinction at exact source lines. No newly reproduced defect is claimed. All earlier ACTIVE entries below are historical.

**2026-09-11 22:33:32 UTC**: helper `/root/replacement_audit` now ACTIVE read-only evidence-to-domain mapping for draft 014 and existing task receipt/variant validators. No code/report/coordination writes, tests or descendants. Lead reads family repository save/rollback seam in parallel; D owns independent generic failure/privacy review. B quota 1 fully used. No command process survives. Prior completed helper work below remains historical.

COMPLETED/released helper `/root/replacement_audit`, READ ONLY on `src/state/usePrototypeStore.ts` replacement branch/recognition consumers and `tests/family-replacement-flow.test.ts`; no owned write paths. Launcher accepted `gpt-6-astra` / `ultra`; Fast not exposed by the launcher. B quota 1 now unused/released after follow-up helper source review: no blocking findings, no edits/jobs/descendants. No later activation-failure rollback claim is made beyond cancellation/final-save cases. Helper finished with no writes/jobs/descendants; the lead report is dirty, so its generic clean-worktree claim is not adopted. Zero descendants. No heavy job, Metro or browser. Completed short commands exited; no retained command PID/session.

## Completed slices and evidence

**B-004 COMMITTED / RELEASED 2026-09-11 22:33:14 UTC**: `a081f642a3241c6d6965da67de4f3438245c7c3f`, parent `b1fc581f3beeab8dcf3f241929fbf5bc29d9cf81`. Exact three paths above. Six runtime lines; four public-action regression cases plus test helpers and complete evidence. Stage list/whitespace inspected, commit exit 0, post-commit tree clean. A may cherry-pick this single fix commit after the already integrated report. All source paths and helper allocation released. Full integrated/browser/native/student gates remain open.

**B-004 final check (22:32 UTC)**: A supplied the missing ignored Expo env at board r5; plain `npm run typecheck` now passes exit 0. Final serial replacement suite passes 10/10 (1.49 s), uncached ESLint and all three changed-file Prettier checks pass exit 0. Prior explicit `--types expo/types` diagnostic also passed. The earlier 1112 short check session completed but its final output was lost at context compaction; the final bounded check was repeated to resolve that evidence gap. Session 85874 completed, no live command. Report records valid RED/GREEN and distinguishes the prior environment failure.

**B-004 checks at 22:28 UTC**: scoped uncached ESLint + Prettier passed exit 0; 6-file focused authority/access/reset set passed 112 tests (5.20 s); separately the two exact private-League suites passed 20 tests (0.837 s), both serial/no-cache. The first command included nonexistent filter `tests/r002b-private-league-recognition.test.ts`, so only SIX files matched; no seven-file claim. Correct route/presentation files then tested explicitly. Plain `npm run typecheck` failed exit 2 at unchanged `BotanicalPressable.tsx:73` (`hovered` missing from native Pressable type). Read-only comparison found main ignored `expo-env.d.ts` references expo/types, while new B worktree has no generated expo-env.d.ts; same component/tsconfig has no source difference. No ungranted file edited. Testing explicit `--types expo/types` as a no-write diagnostic; A needs to restore normal generated environment for worktree validation. Sessions 40014,73991,39723,61861 complete; no retained job.

**B-004 GREEN** at 2026-09-11 22:27:30 UTC: same focused replacement command exit 0, 10/10 tests, 1.53 s. Six added store lines recreate replacement League from new Growth epoch and clear old reveal commitments only in successful replacement. New tests cover before/after recognition, full new-family recognition + exact separate counters/lifetime, duplicate no-op, cancelled/failed-save preservation. Test session 72596 complete. Preparing scoped lint/typecheck, bounded related regression set and helper diff review; no full suite/browser/native job.

**B-004 RED reproduced** at 2026-09-11 22:27:09 UTC: `npm test -- tests/family-replacement-flow.test.ts`, exit 1, 2 failed / 8 passed, 1.51 s. With no old recognition, League epoch remained old and new-family applyRecognition failed; with old recognition, stale receipt/commitment survived, League stayed 5/5/100 and restoreCheckInState failed. Existing cancellation/failed-save plus new recognized-authority preservation cases passed. Runtime store has not yet been edited. Initial harness run at 22:26:47 (4 failed / 6 passed) exposed a missing prepared-Guide acceptance step and wrong test-only Leaf field; those harness issues were corrected before the valid RED run. No production behavior changed to obtain RED. Both short test sessions 99967/60875 finished; no live job.

**B-001 complete, report RELEASED**: commit `b1fc581f3beeab8dcf3f241929fbf5bc29d9cf81`, required base `02b9618631fa9fc1b29f2cda5fa68c6adb2003fd`, exact path `docs/competition-readiness/workstreams/b-recovery-audit.md`. One report, 230 lines; no runtime change. Scoped Prettier write/check, Python local-link/primary-source checks, staged `git diff --cached --check`, exact staged-path inspection and commit all exit 0. Student review PENDING; automated app/browser/native NOT RUN. Helper source review complete/released, no jobs. Only pre-existing/A-provided node_modules symlink remains untracked in B worktree. Report is re-held solely for newly granted B-004 evidence.

Startup history: Startup inspected Git status/branch/HEAD, registered worktrees, canonical protocol/board/all statuses, constitution and current ownership. Intended B worktree reads failed because the directory does not exist. Existing two worktrees are main Ghaf and Ghaf-r002-reconciliation-20260904; neither is B's assigned worktree.

## Findings and decisions needed

- B-F001: no active A run, B grant or B worktree exists at startup. Read-only analysis can proceed at the recorded integration baseline; implementation needs A's activated grant and accepted Spec Kit story.

### B-F002 — current journey and recognition authorities (source trace, not a fresh runtime pass)

All references below are relative to `/home/smyk/projects/Ghaf` at `02b9618631fa9fc1b29f2cda5fa68c6adb2003fd`.

1. Parent selection/Guide: `src/components/family-growth/ParentTaskComposer.tsx:130` constructs the profile category plan; `:201` limits executable selection to Salem + Green Impact + the canonical recycling template; `:224` calls the guarded store draft action. Store `src/state/usePrototypeStore.ts:2845` creates the draft, `:2907` requests the prepared Guide with stale-request checks, `:3203` reviews it, and `:3253` approves assignment. Repeated approval checks the exact assignment/task/version/Child/choice before returning unchanged.
2. Child chooses from Today at `app/child/index.tsx:289` through store `:3305`; store `:3329` starts the chosen task. The active Parent/Child guard functions at store `:884` and `:895` require controller authority, not merely a persisted role value. `app/child/task.tsx:192` requests bounded Coach help. Submission at `:290` supplies `permitted_help` and prepared help/facts, then store `:4248` and deterministic task service validate the active Child/journey. It grants no progress.
3. Parent retry is store `:4267` / `:4279`: only journey/confirmation state changes; no counter deduction. Praise planning is `src/components/family-growth/ParentCheckIn.tsx:176` and store `:4527`. Recognition is a distinct later Parent action at component `:219`, guarded in store `:4640`; the provider requires different presentation/continuation action IDs (`src/services/mock/index.ts:1020`). Reopening is not that action.
4. `src/services/mock/index.ts:1035` consults the existing receipt before projecting growth. New recognition evaluates reward rules, filters Green privacy (`:1094`), computes Seeds/landscape and constructs immutable receipt provenance (`:1137`), then returns one next session (`:1185`). The store independently validates provider output and cross-authority parity before the single `set` at `:5033`. It also derives independent Growth, Family Reward, private League and reveal authorities; no screen balance is authoritative. `src/features/tasks/recognitionSession.ts` and `recognitionProviderBoundary.ts` are reusable validation seams, not persistence adapters.
5. Separate default oracles: personal Seeds 48→60, Mangrove 48/Shoot→60/Sapling, cooperative canopy 19/25→20/25, Green Circle 11/12→12/12, private League Salem 4/5 & 80→5/5 & 100. Alya's personal 36 stays unchanged. Source: `src/features/tasks/demoContent.ts:818`, `:830`, `:838`; `src/services/mock/fixtures.ts:323`, `:332`; `src/features/league/presentation.ts:11`, `:51`. Family Reward has its own eligible 108/120→120/120 authority (`src/features/family-hub/index.ts:25`, `:79`); the gated lifetime fixture is separate from default personal Seeds. No restoration or memory may combine these authorities.

### B-F003 — reload loss and existing storage seam (confirmed source; historical browser evidence)

- Startup restores the directory (`src/state/usePrototypeStore.ts:366`) and remembered device preference (`:403`), but gets a fresh prototype session at `:445`, builds Growth at `:452` with reset sequence 0 and fresh other runtimes. `src/services/mock/fixtures.ts:286` begins with no journey/assignment/receipt and the opening counters. The service registry (`src/services/index.ts:109`) supplies only directory, device affinity, ambience and saved Parent-template repositories; it has no task/progress repository. Saved templates and audio preferences are also durable, but are not completion evidence.
- Prior browser QA at `55f9f2b` observed League 100→80, Mangrove 60→48 and canopy 20→19 after reload. Read artifacts: `output/competition-readiness/qa/reload-result.txt` and `reload-garden-and-guard.txt`; attribution remains historical, not B runtime evidence. `git log --oneline 55f9f2b..HEAD -- src/state/usePrototypeStore.ts src/services/local src/features/tasks src/features/assistants/profilePersonalization.ts` exited 0 with no changes in these scoped authorities. This does not create a new browser/native pass.
- Reuse `src/services/local/storageTypes.ts:1`: synchronous get/set/remove; native SQLite sync adapter `storage.native.ts:5`, web localStorage `storage.web.ts:10`, deterministic memory/fail-next-write adapter `storage.ts:7`. Individual repository writes are validated record writes; the interface exposes no multi-key transaction or async cancellation guarantee.
- Family schema 4 (`src/models/localFamily.ts:16`) supports explicit prior-schema migrations through `src/services/local/repository.ts:48`; corrupt/unknown/current data does not become trusted state. New progression needs its own strict versioned minimum-evidence parser and error dispositions, not serialization of the Zustand store or a numeric balance.

### B-F004 — reset/generation contract prerequisites (source findings)

- Parent-only reset (`src/state/usePrototypeStore.ts:2589`) clears affinity, directory, ambience and saved templates in sequence, returns on each error, then recreates controllers/domain/reveal state and signs out in Arabic. It increments runtime resetSequence; it does not persist that sequence. Multi-key interruption can therefore leave some keys cleared before the later operation fails. This is a source-based transaction risk to define/test, not a newly reproduced reset defect.
- All replacement families reuse `household_al_noor` and the same Child IDs (`src/models/localFamily.ts:43`). Family creation uses fixed `R003_LOCAL_FAMILY_TIME` (store `:284`, `:1681`). Runtime profile epoch generation is `prototype-reset-<sequence>:<Child>` (`src/features/growth/bootstrap.ts:82`), and startup initializes sequence 0. Neither these fixed IDs nor timestamp alone safely binds new durable evidence across replacement/reset/restart.
- A's story must define a persisted synthetic family/reset generation and its fail-closed ordering across directory replacement, evidence saves and reset. Old pending work must compare its generation at application/commit time and never recreate deleted evidence. Minimum evidence excludes credentials, controller sessions, permission grants, provider data, Child drafts/reflections/help text/media/transcripts. Restoration must reconstruct a recognized result without reopening celebration or calling a new award action.

### B-F005 — actual recommendation basis and rationale seam (source trace)

- Prepared Guide (`src/services/mock/index.ts:1299`) accepts only bounded synthetic P0/version-one/Green-Impact/9–11 requests; it returns `guide_recycling_refine_v1`, retains Parent wording and requires Parent choice. It is not a general live coach.
- Category helper `src/features/assistants/profilePersonalization.ts:149`: nature/sustainability/gardening → Green; stories/reading/puzzles or `simpler_instructions` → Learning; family_helping → Home; making/drawing → Kindness; fallback Home; stable de-duplication and at most two recommendations. Custom interest/hobby text is reduced by bounded regexes (`:109`, `:131`), not a model. Custom support/accessibility affects coaching style. Opt-out returns no recommendations; `:246` validates all eight categories exactly once, preserves the remaining order and labels prepared/local/fallible/no provider.
- The plan input currently includes sex (`ParentTaskComposer.tsx:136`); the helper uses it for grammatical address, not category ranking (`profilePersonalization.ts:228`; existing regression `tests/required-child-profile-personalization.test.tsx:175`). Nicknames/relatives are absent from this input. Current `PRODUCT.md:254` acknowledges custom fields/grammatical address, while older FR-216/FR-194 wording excludes free text/gender. Do not silently change ranking from that older paragraph or claim all current inputs are curated-only. A's accepted rationale story must reconcile the current custom/accessibility inputs with the latest no-sensitive-content requirement, avoid echoing raw text, and keep gender/identity out of the rationale projection. Any ranking change is a separately explicit contract decision.

### B-F006 — memory remains absent (source inventory)

No memory/timeline/journal/recovery-named module/route/repository exists in inspected `app`, `src/features`, `src/models`, `src/services/local`; registry/startup has no durable memory authority. Internal recognition evidence and reveal queues do not establish a user-facing durable journal. `src/features/family-connections/index.ts:180` produces private recognition-only Parent ideas; PRODUCT Feature 008 says they cannot be accepted/completed/confirmed/rewarded. A separate story must define one private minimal projection from accepted confirmation, audience, retention/deletion, same-family/profile binding, idempotency and zero progression. No general timeline or Child-content archive is implied.

### B-F007 — independently confirmed source omission; runtime reproduction NOT RUN

`src/state/usePrototypeStore.ts:1701` computes replacement reset; its success merge at `:1769` clears session, Growth, Family Reward and reveal queue but does not reset `privateLeague` or `approvalRevealCommitments`. Full prototype reset does reset both (`:2625`). Because Zustand merges partial state, old secondary authorities can remain. Proposed trigger: fully recognize recycling, replace the family, inspect League and attempt the new family's task/confirmation. Expected: fresh independent baseline and no old reveal fingerprint; suspected: retained old League receipt/commitment causing stale display or validation rejection (`:4533`, `:4647`). Existing replacement test `tests/family-replacement-flow.test.ts:197` pairs/changes permission before replacement but never recognizes first and asserts neither authority. Request A's exact reproduction/fix boundary; no code or regression has been written and no runtime failure is claimed.


## Outbox

1. **B-20260911T2217Z-001 → A — activation/grant request (RESOLVED by A revision 1).** Startup at board revision 0 found no active code/report grant and no `/home/smyk/projects/Ghaf-demo-systems`. Please activate the board, prepare or explicitly assign B's worktree/base, grant `docs/competition-readiness/workstreams/b-recovery-audit.md` for the read-only findings, and allocate a helper if independent audit work is desired. New persistence/rationale/memory code requires the accepted committed Spec Kit story and exact files. B is continuing read-only source tracing without claiming any planned row READY. Baseline `02b9618`; no heavy job requested yet.

2. **B-20260911T2217Z-002 → A — AUDIT HANDOFF (OPEN).** Findings B-F002–F007 above are available now at baseline `02b9618`; use them for B-001/A-002. Priority: approved restart continuity, durable family/reset generation, praise-before-award restoration, separate authority replay, and truthful rationale inputs. No code grant is assumed. TEAM_OWNERSHIP now records your active startup, so B will continue useful review while the real grant is prepared.
3. **B-20260911T2217Z-003 → A — PROPOSED CONTRACT/SLICES (OPEN, not self-approved).** First commit the minimum recovery story defining allowed stages (assigned/chosen/in-progress/submitted/recognized), minimum synthetic evidence, generation binding, supported schema migration, storage error/retry UX, reset/replacement crash ordering and consumed celebration policy. Suggested separable B files after exact grant: `src/features/progress-recovery/evidence.ts` + `tests/progress-evidence.test.ts`, then `src/services/local/progressEvidenceRepository.ts` + `tests/progress-evidence-repository.test.ts`. A retains store/registry/startup/reset/local-family integration or explicitly transfers exact files. Acceptance must include duplicate confirmation; wrong role/Child/family; reload; corrupt/unknown version; failed/interrupted writes; replacement using reused IDs; repeated reset and stale work; exact separate deltas and no restored celebration/memory. These paths are proposals only. Do not silently broaden existing storage or make a memory leaf an authority. Rationale and memory need their own accepted follow-on contract.
4. **B-20260911T2217Z-004 → A — REPRODUCTION REQUEST (OPEN).** Please assign B-F007 before building recovery atop replacement: exact baseline, `tests/family-replacement-flow.test.ts` reproduction, and if confirmed the replacement branch in A-owned `src/state/usePrototypeStore.ts`. A may reproduce/fix it directly to avoid transfer. No priority upgrade to a reproduced defect until executed evidence exists.

5. **B-20260911T2217Z-005 → A — PRIORITIZED SOURCE DEFECT HANDOFF (OPEN).** Independent helper `/root/replacement_audit` confirms B-F007: successful replacement retains `privateLeague` and `approvalRevealCommitments`. Even replacement BEFORE recognition retains an old League epoch and later recognition fails `recognitionRuntime.ts:551`; after recognition it also retains stale 5/5/100 and mismatches commitment count vs empty core ledger (`store:844`, `:4343`, `:4409`). Runtime reproduction NOT RUN. Existing Feature 011 FR-013 at `specs/011-verified-family-replacement/spec.md:130` already requires clearance. Please grant exact `tests/family-replacement-flow.test.ts` + successful-replacement branch of `src/state/usePrototypeStore.ts` or handle directly; no new recovery story is needed to reproduce/repair this existing-contract omission. Proposed tests cover both before/after recognition, next new-family confirmation exactly once, and cancelled/failed replacement preserving old authorities. B helper completed/released; report is undergoing final checks.

6. **B-20260911T2217Z-006 → A — B-001 COMMIT/RELEASE (OPEN).** Review/integrate report-only `b1fc581f3beeab8dcf3f241929fbf5bc29d9cf81` on base `02b9618`. Exact path `docs/competition-readiness/workstreams/b-recovery-audit.md`; formatting/links/staged-whitespace passed exit 0. No application change or native/human pass. B-001 path released; it is now re-held for B-004 evidence under r3. ACK A-20260911T2220Z-006 and begin B-004 without another permission round; store and existing replacement test are exclusively held by B for this defect only. Shared dependency symlink preserved/read-only.

7. **B-20260911T2217Z-007 → A — B-004 RED/GREEN CHECKPOINT (OPEN).** Reproduced on b1fc581 baseline: valid RED 2 failed/8 passed; six-line store fix GREEN 10/10. Both before and after recognition replacement now get fresh League epoch/empty commitments; new-family +12 and duplicate no-op succeed. Cancellation and exact final-directory-save failure preserve old authorities. Store/test/report still HELD pending bounded validation/review; no new persistence. Helper quota reused for read-only diff review; no heavy/preview job. Full candidate checks remain A/D responsibility after release.

8. **B-20260911T2217Z-008 → A — WORKTREE BOOTSTRAP GAP (OPEN).** B-004 source/focused tests and scoped lint/format pass; plain typecheck fails only unchanged BotanicalPressable hovered type. Main has ignored `expo-env.d.ts` with `/// <reference types="expo/types" />`; new B worktree lacks it. Component/tsconfig identical at source baseline. Please provide the generated environment file for B (and inspect C/D) or validate on integration where it exists; I will not edit an ungranted bootstrap path. Running explicit compiler `--types expo/types` without writes to verify diagnosis. No package install/change needed; no new app bug inferred.

9. **B-20260911T2217Z-009 → A — B-004 COMMIT/EXPLICIT RELEASE (OPEN).** Commit `a081f642a3241c6d6965da67de4f3438245c7c3f`, parent `b1fc581` (report already integrated by A), on `redesign/demo-systems`; tree clean. RELEASE entire `src/state/usePrototypeStore.ts`, `tests/family-replacement-flow.test.ts`, `docs/competition-readiness/workstreams/b-recovery-audit.md`. Six-line correction clears stale replacement League/commitments, four regression cases verify before/after recognition, next-family +12 once, cancellation/final-save preservation. RED 2 fail/8 pass; GREEN 10; 8 relevant files/132 tests pass; scoped lint/format and now plain typecheck pass after your r5 bootstrap. Helper review no blocking findings; native/browser/student NOT RUN/PENDING. No helpers/jobs retained. Please integrate/retest exact commit; B will review draft 014 read-only while acceptance/grants remain pending. No persistence/memory/rationale capability added.

10. **B-20260911T2217Z-010 → A/D — 014 REPOSITORY SEAM REVIEW (OPEN; read-only proposal support).** Source snapshot main `152b053`, draft014 unaccepted. D-014-06/07 already identify orphan cleanup and the incorrect T006 filename; no duplicate defect claim. Concrete legacy branch to cover: `src/services/local/repository.ts:56-98` falls back from absent current key to v3/v2/v1 and writes a migrated family; interrupted migration can leave the old key (`:93-96`). Removing the envelope first while leaving any old key can therefore resurrect a retired family unless the approved tombstone/orphan policy suppresses ALL legacy migration/repair reads, not merely progress restoration. Existing clear `:167-172` deliberately clears legacy keys first. Test coexistence of current + old key, reset commit then crash, legacy-read/repair attempt and restart. Current `save(record)` is an unconditional whole-directory overwrite (`:101-109`); callers needing exact expected-generation/revision adaptation include creation/repair and rollback (`store:1685-1711`, `:1835`), pairing/revocation (`:1987/2000/2023/2369`), locale (`:2539`), and repository `setPairedChild(:154-165)`. Rollback must preserve the prior complete envelope/revision, not just the existing `previousFamily` projection. No runtime change, no new grant requested beyond your accepted-story gate. Helper is mapping task-validator seams separately; D's existing issues remain authoritative review items.

11. **B-20260911T2217Z-011 → A/D — 014 PURE VALIDATOR MAP / HELPER RELEASE (OPEN).** Read-only helper `/root/replacement_audit` finished; lead reviewed source warning. Existing seams: `tasks/validation.ts:400/413` resolve/validate exact canonical/variant content but do not prove past Parent/Child acceptance; `tasks/lifecycle.ts:79` validates transitions but trusts supplied task validity; `tasks/recognitionSession.ts:605` validates exact timeline; `:1480/1605/1746` validate check-in/confirmation candidates; `:2134` validates historical recognition transition; `:235/300/387` check Growth/Seed/achievement parity. Every receipt member at `models/familyGrowth.ts:362`, including explicit null consequences, must be reconstructed. Provenance at `:346` includes task/version/submission/profile/landscape, completion mode, recurrence/routine counts, separate Reward/Leaf eligibility and privacy/recognition context. Pinned resolver may reconstruct constants/IDs, never trust stored totals. Crucial reuse limit: `zeroSeedDuplicateMatchesPolicy(:1122)` returns true for non-null Seed transactions for that check; do NOT treat the already_confirmed branch alone as a disk policy validator. The applied branch recomputes positive award at `:1987` and compares exact expected receipt at `:2084`; proposed restore adapter must validate that historical transition purely, without invoking the award command, then explicitly project consumed/static presentation (applied validator expects unconsumed at `:2127`). Existing planning `:1688` can regenerate a continuation from praise_presented, so pending restore needs approved editing_praise/no-continuation mapping and fresh presentation. Provider envelope validator (`recognitionProviderBoundary.ts:136`) is structural only. Still needs exact pinned policy/version/decision schema and approved option-ID reconstruction; no implementation or new defect/native claim. Helper allocation fully RELEASED; no source locks or jobs. All current B grants complete; next code work requires accepted014 plus exact grant, or a reproduced existing-contract defect assigned by A.

12. **B-20260911T2217Z-012 → A — FINAL BATCH HANDOFF / CHECKPOINT ACK (OPEN).** All B grants completed: audit `b1fc581`, repair `a081f642a3241c6d6965da67de4f3438245c7c3f`; exact store/test/report released and A integration acknowledged. B worktree clean; no live helper/descendant/PID/job/heavy/preview allocation. B-010/011 read-only recovery maps are delivered; A helper now owns remaining T004 feasibility and D review stays independent. Human/Android acceptance PENDING/NOT RUN; 132 relevant tests, plain typecheck and scoped lint/format passed on B candidate. B-002/003 remain blocked on accepted contract/exact grant, with no current READY B task. ACK A-013 and pause status writes for your checkpoint. Resume this session for a concrete next grant; do not infer files send a wake-up. No further source changes or status writes until resumed.

## Acknowledgments and responses

ACK **A-20260911T2220Z-013** at 2026-09-11 22:36:43 UTC: safe boundary reached, status updated and writes PAUSED now for A snapshot/staging. No B source/report/helper/job remains held. A alone may stage this file; all pending/outbox history retained. Source integration ACK for B-009 and received B-010/011 noted from A status at r7. Do not treat the board's older B-held sentence as a live lock: explicit B-009 release and A acceptance control. This is a resumable end-of-batch handoff; a later file grant does not wake this turn automatically. Resume B after A RESUME and an eligible exact task, inspecting actual Git/status first.

ACK **A-20260911T2220Z-009** and board r5 bootstrap: plain typecheck verified pass after A supplied ignored ambient types. B-007/008 are acknowledged by A r5; retain addressed history. Board r6 Arabic-duration change belongs to A, not B. Draft 014 remains read-only/unaccepted.

ACK **A-20260911T2220Z-006** at 2026-09-11 22:25:25 UTC: board r3 B-004 transfer accepted after B-001 commit; store/test/report held, reproduce before fix. A acknowledged B outbox 002–004 in r3; retain them above as addressed history.

ACK **A-20260911T2220Z-001** at 2026-09-11 22:22:20 UTC: B-001-r1 and board revision 1 accepted. Worktree/branch/HEAD verified clean. Report boundary held by B lead; one helper quota accepted, used and now released after independent read-only review. ACK **A-20260911T2220Z-004**: QA-04 route repair noted at board revision 2; B preserves baseline source audit and owns no route. Startup request B-20260911T2217Z-001 was acknowledged by A; retained above as history, now RESOLVED by revision 1.

## AI assistance and review

Actual user instruction excerpt: “If the queue has no code grant, begin the read-only gap audit and publish findings or proposed tasks in your outbox.” The user also requested the existing LOCAL Parent selection/approval → Child acceptance/help/completion → Parent confirmation → praise/growth trace with current authorities, reset, reload loss, prepared recommendation basis and absent private memory behavior. Generated contribution so far: this status and startup findings only. No application generated. Exact full task prompt is the supplied Session B role text, preserved as `docs/competition-readiness/orchestration/session-b-demo-systems.md` at baseline `02b9618`, Git blob `aa81893a4273f2507666a0700d09312f9031f28c`. No follow-up user/helper prompts or generated code. Generated additions: source trace, risk analysis and proposed bounded slices in this status. Rejected/deferred approaches: raw-store/balance persistence; treating fixture IDs as a unique replacement generation; treating a planning row as a grant; fabricating model settings/native/human evidence. No helper suggestions received. Student ownership and exact-output review remain PENDING.

### Actual read-only follow-up prompts after B-004 release

```text
Read-only follow-up while B-004 is released and A actively drafts Feature 014. In /home/smyk/projects/Ghaf (current main source; draft not accepted), inspect exactly specs/014-local-progress-recovery/{spec.md,data-model.md,contracts/recovery-v1.md} and existing src/features/tasks/{recognitionSession.ts,recognitionProviderBoundary.ts,demoContent.ts}, src/models/task.ts (or actual task type file), plus narrowly necessary accepted-variant/receipt definitions. Output a minimal evidence-to-domain mapping for canonical task, permitted help, pre-accepted smaller variants, pending confirmation and recognized restoration: existing pure validators usable, fields that cannot be discarded, unresolved exact schema decisions. Do not repeat D's generic corruption/reset review. No code/report/coordination writes, tests, browser, heavy jobs or descendants; others are editing, preserve their work. No runtime grant exists. Cite exact current file lines and distinguish proposals from current behavior. Lead will independently inspect family repository commit seam and publish concise outbox. Explicitly release helper allocation when finished.
```

```text
D has already reported closed allowlist/private text exclusion; canonical @1 +12 vs smaller @2 +8/safe @2 +12 with different League/Reward; pending parent_review_required/child_decision_required states. Do not redo those findings. Focus only concrete existing pure validator/projection entry points and minimum receipt fields needed to satisfy them (implementation seam map). A draft status remains blocked.
```

Final helper instruction:

```text
Useful concrete seam warning. Please finalize a concise actionable map with this policy distinction, exact existing entry points and mandatory provenance fields. No need exhaustive field dumps; preserve unresolved decisions and no reproduced-defect/native claims. Then release allocation. Lead will publish through B outbox; no additional test or implementation scope.
```

Generated contribution after release: B-010 repository/source mapping and B-011 pure-validator map. Helper output reviewed and accepted as proposed implementation guidance. Rejected shortcut: duplicate-only receipt validation. No application/report changes, runtime test or new feature after release. Student review PENDING.

## Recovery cursor

Completed B-001 `b1fc581` and B-004 `a081f642a3241c6d6965da67de4f3438245c7c3f`; B tree clean, exact store/test/report released and A acknowledged integration. Helper and every job stopped/released. B-010/011 contain reviewed recovery seam guidance; A helper now owns remaining T004 feasibility. No eligible B code grant exists; recovery story014 is still DRAFT. Status writes PAUSED under ACK A-013 for A-only coordination checkpoint. This turn is ending as a resumable handoff. On operator/session resume, read A's RESUME and canonical BOARD/all statuses, verify actual branch/HEAD/dirty files and helper/job state, then ACK the next accepted exact grant before edits. Never reuse released B-004 ownership or self-approve persistence/memory/rationale. A/D own integrated candidate checks; native/student acceptance stays open.
