# Release execution — 2026-09-14

Owner: this root session, continuing the animation/cleanup thread under the user's
explicit release execution and conditional publication authorization. Starting
HEAD `baf88a0`, dirty shared main. Feature020 migration and NAV-MOTION retain every
existing reservation. No reset, secret rotation, purchase or private-row dump.

RELEASE-001: root reserves `specs/021-release-readiness/`,
`docs/competition-readiness/release-readiness.md`, this status and ignored
`.expo/release-20260914/` evidence. One bounded helper `motion_flow_audit` initially
audits release tooling read-only. No descendants or independent heavy jobs.
Root owns its own browser tabs and emulator input; Feature020's one DB/build lane
is preserved. Production inspection starts read-only on the verified linked
project `bqcfynlbxevqlzbkimhy` / ghaf-parent-pilot / main PRODUCTION. The separate
messaging project is not silently substituted or merged.

RELEASE-002 to SUPABASE-MIGRATION: user has authorized full release hardening and
gated publication. Your active source/migrations remain yours. Root is auditing
the deployed Dashboard (22 Security Advisor warnings; backup overview says none),
release access, policy/deletion, and disjoint CI provenance. Please expose your
coherent commit and exact hosted/local test checkpoint before release/native
integration. No competing production mutation or heavy DB job from this lane.

RELEASE-003: helper is granted only `.github/workflows/android-internal.yml` and
`scripts/native/validate-hosted-build.{mjs,test.mjs}` to require source checks before
building and enforce version/configuration provenance. Preserve internal/debug
signing identity and explicit existing delivery mode; do not activate Play, change
dependencies, push, dispatch or alter Feature020 flags. Root integrates/tests.

RELEASE-004 to SUPABASE-MIGRATION: P1 public-onboarding gap found in current source:
getAccess still requires pilot_access.status=approved, and family_owner authorization
joins that approval. HTTP QA manually approves users with admin credentials. This
cannot satisfy the owner's new no-manual-database-editing public account journey.
Please preserve suspension, verified adult/anonymous Child boundaries, but expose
a reviewed public-onboarding contract or release blocker; root will not overlap
your account/controller files. Account-deletion initiation/external request path
are also absent (no implementation matches); release policy gate remains blocked.
Production metadata/advisors query is read-only; no private user rows were read.
Root will keep release-only backend hardening separate from your deployed queue.
RELEASE-005: root source typecheck fails only at tests/access/family-messaging-hosted.integration.test.ts:509: plain string passed to inferred crypto.randomUUID template-literal type. This file is Feature020-owned; please correct the intended request-id annotation (do not weaken assertions). Root CI validator 11/11 and repo:check 5/5 plus repository policy checks pass. Backup detail confirms Free Plan has no project backups, not merely missing UI data.

RELEASE-006: CI slice committed 15182d0, exact three reserved files released.
Helper owns workers/ghaf-ai-gateway/src/index.ts and tests/gateway/bounded-ai-gateway.test.ts,
tests/gateway/bounded-ai-mcp.test.ts for fail-closed missing replay binding.
No provider deployment or flag activation. Root integrates focused checks.

RELEASE-007: root reserves app.config.ts and tests/platform/android-runtime-readiness.test.ts
only to block unused SYSTEM_ALERT_WINDOW, confirmed in existing fresh APK and
Expo base manifest; validator gets an artifact-level denial regression.
Microphone/biometric settings retained pending feature-specific review.
Emulator input reserved: new Android user11 GhafReleaseQA20260914, isolated from
user0 real adult session and prior user10. Baseline existing APK only; no data clear,
no original package replacement, no Feature020 native pass inferred.

RELEASE-008: root native baseline verifies installed SHA matches 5ad7faa APK,
version0.1.0/code1, exact package, isolated Android user11 UID1110209. Arabic
sign-in rendered; English switch/scroll and keyboard Back executed. Owner-provided
account sign-in failed with network_unavailable; emulator DNS cannot resolve the
project although Windows can. Root gracefully restarted same AVD with documented
-dns-server 8.8.8.8,8.8.4.4, no snapshot load/save; userdata retained. No claim of
successful real login/current Feature020 native acceptance. Root reserves emulator.
Root has not pushed others' new commits; awaiting coherent integrated source checkpoint.
Helper release-security-and-privacy.md is complete; no provider deployment.

RELEASE-009 to Feature020 integration: latest migration inventory read at 23:34
shows local checks/hosted001-014 and current browser in progress, native blocked on
20GB local preflight. GitHub CLI here is authenticated and Actions internal Android
workflow accessible; remote fresh Gradle build avoids that local disk constraint.
I can dispatch from your coherent verified commit after you release the source
checkpoint. No new EAS/platform/signing identity needed. New verify job gates APK.
Emulator restarted successfully with explicit DNS; Supabase hostname now resolves.
Root baseline oldAPK auth retry is in progress using owner-authorized account,
with resource-ID-only state collection and no captured private household content.

RELEASE-010: coherent Feature020 runtime c54f25f received. Root current typecheck
passes; candidate includes release fixes15182d0/bb75cbb/a6b57f6. Root will push
an isolated release/021-internal-20260914 ref for a fresh existing GitHub Gradle
workflow; its source-check job must pass before APK creation. No main merge/store
submission or production signing change. Remote build is this root's lane.
Owner account native sign-in now PASSED on baseline5ad7faa after explicit emulator
DNS fix; resource IDs only, no household capture. Force-stop restoration underway;
after that root releases emulator to Feature020's accounttest2 checks until fresh
APK integration. Preserve new Android user11 and original user0/user10 data.

RELEASE-011: emulator input now RELEASED to Feature020. Baseline user11 local
sign-out confirmed, timeout restored60000; original user0 foreground restored
without capturing it. No original session/data altered. Root will coordinate fresh
APK installation after remote build34887712753; current CI lint/format pass,
full tests running. Root three signed-out process-cold am start samples:
5649/3995/4056ms to activity display (median4056), not authenticated usability,
physical-device timing or FPS. Separate fresh-user firststart15068 and postboot8158
are different conditions and excluded from that comparison; host16GB shared.
Root read-only DB aggregates:13,806,739bytes,16connections; named hourly message
retention job active. Aggregate query timings are mixed administration/QA, not
controlled product API/load measurements. No private query text or rows exposed.
Helper temporarily owns full release-readiness.md ledger; root won't edit it.

RELEASE-012: first fresh workflow34887712753 correctly stopped before APK creation:
3387 tests passed, one Gemini fixture failed (missing newly required replay binding),
six skipped. Feature020 committed the fixture correction d13c148; root verified
all107 gateway tests across8files, then fast-forwarded isolated release ref and
dispatched34888514880. Do not repeat the failed candidate or call either APK built.
Root directly refreshed hosted migration list: all19 local/remote versions match,
ending20260915001400. Recovery email preview is an OTP with one-hour expiry;
approval/pilot copy still reflects restricted access, not public family onboarding.
Root reserves release-qa-results.md, release-performance.md and
release-deployment-runbook.md in docs/competition-readiness/ for sanitized evidence
and operational checkpoint only; no overlap with Feature020 backend documentation.

RELEASE-013: root reserves package-lock.json only for compatible js-yaml4.3.1→4.3.2
security patch GHSA-2883-xcg3-v3hh. Existing consumers accept^4.1/^4.3. No
package.json/framework changes, no node_modules mutation during another lane's
native rebundle. Remaining moderate dependency chains will be triaged explicitly.

RELEASE-014: f129b7f committed only the compatible YAML lock update. Isolated4.3.2
install rejects the bounded empty-merge reproduction accepted by4.3.1; all3workflow
parse results match. Locked audit now0high/14moderate. Shared node_modules unchanged.
Release ref fast-forwarded; native34888514880 remains exactlyd13c148. Its source
job PASSED, as did independent backend34888514402 and repository34888514386.
Current native Gradle is running; no APK acceptance yet. Helper released ledger
and security docs; now owns only new release-task-catalog.md, source-only24task
participation/coverage audit. No helper jobs/device/DB/source edits.

To Feature020: please explicitly release emulator input after accounttest2 native
checks, and provide a retained synthetic QA fixture identifier safe for root's
fresh-APK isolated-user11 normal journey. No original real records will be edited.
RELEASE-004 public approval and deletion remain P1 release blockers; your auth/
backend ownership is respected. Root's new runbook records these without hiding
missing calendar/location/allowance/calls or implying a final release decision.

RELEASE-015: catalog audit found missing safety fields in real TemplateDetails,
and visible assigned-state Help rejected by SQL (only accepted/in_progress).
These are separate from the larger missing authoritative prerequisite/retry
contract. Root reserves src/components/cloud-family/CloudTaskViews.tsx,
src/i18n/cloudFamilyResources.ts, new tests/cloud-family-ui/task-safety.test.tsx,
new migration20260915001500_family_preaccept_help.sql and its new SQL test for
the bounded safety-copy + assigned-help repair; no model/auth/growth owner overlap.
Feature020's latest release record says helper code boundaries released. Existing
native34888514880 remains an earlier candidate. No hosted mutation until local
tests, dryrun, compatibility and release-lane coordination; no retroactive safety
acknowledgment or claim that skipped adult duties became verified.

RELEASE-016: Help repair committed e8c6b3b. Before migration,8of24focused SQL
checks failed on invalid_transition. Loopback-only migration application/repeat
then passed24/24 plus73core checks. The local CLI history was deliberately not
edited; its later migration-up can safely apply the repeatable migration.
Hosted target reverified; dryrun listed ONLY015, no seed/role changes. Captured
existing core-function DDL (no row data) for compatible corrective recovery;
015 deployed successfully to bqcfynlbxevqlzbkimhy. This preserves grants/identity/
revision/receipts and changes only assigned-state help eligibility. Hosted normal
restricted-session smoke remains pending; full database/media recovery still blocked.
Safety UI9tests passed by helper; root integration typecheck/core-ui/lint underway.
Emulator released by Feature020; root acknowledges isolated synthetic fixture14222
and owns user11 input again when fresh APK arrives. No user0 real-data capture.

RELEASE-017: root reserves this release entry in ai-assistance-ledger.md and the
current-build section of docs/backend/internal-android.md for eventual artifact
handoff, preserving historical evidence. Helper owns only release-asset-notices.md
for shipped license/provenance review. Source63353ae frozen for candidate34889994736,
queued behind d13c148 build34888514880. Backend63353ae CI passed. Hosted Help smoke
passed five normal restricted-session checks; invocation14222 gained one assigned
GI01 with Help requested, no award/history mutation. Parent/Child SDK sessions
signed out locally. Read-only hosted result confirms20migrations through015 and
restored hourly retention with one successful scheduled run. No full backup claim.

RELEASE-018: native d13c148 standalone upgrade/login reproduced a three-row footer
at720x1600/font1, using384px before insets and obscuring most child actions until
scroll. Root reserves ONLY CloudFamilyView.tsx for a bounded equal-column footer
repair, retaining all six labeled destinations, existing press motion and Back.
No NAV-MOTION files overlap. Source63353ae remains a separately identified build;
this later repair requires its own source checks and fresh artifact acceptance.

RELEASE-019: b2 native default footer passed, but font1.5 broke the Arabic Settings
word across lines. Root extends the same CloudFamilyView.tsx boundary plus
tests/cloud-family-ui/core-ui.test.tsx for responsive wider columns at enlarged
text/narrow width. Selection state, six labels and press/Back behavior remain.
Fresh artifact and large-text acceptance required; no NAV-MOTION edits.

RELEASE-020: root reserves release-evidence/2026-09-15/ for a small reviewed
synthetic-only native screenshot set and its source/limitations index. No real
account, credentials, pairing tokens or user0 captures will be included. Helper
owns only release-store-listing.md and release-asset-notices.md for reconciliation
against recorded b2 evidence; final4dd native acceptance is still pending.

RELEASE-021: final4dd build34896135774 and backend/repository CI passed. Root
matched downloaded and installed APK hashes; exact retained template signer,
ZIP and50-library ELF checks passed. Native font1/font1.5 Arabic/English footer,
repeated tabs/Back, cancelled press and zero-scale restart passed. Child upgrade
and Parent restoration from clean signed-out synthetic QA app data retained the
existing16Seeds/two canopy/two memories. Both profiles signed out and device
settings restored; emulator remains running. Warmed measurements are mixed,
with no physical/performance acceptance. Helper released all document scopes.
Final source4dd649025da828a53ee9617573ebbc0b2849245c; APK SHA
eacad1cc78a04e4d2a7d753a646ea07bb0540061e7e0fd47497ad757afd3a886.
Publication stays blocked by the canonical45-row/eight-gate ledger. This is a
coherent execution checkpoint, not background work or public release approval.
NAV-MOTION source and tests remain untouched/uncommitted by this lane.
