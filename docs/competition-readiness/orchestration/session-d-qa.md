# Session D — independent QA and rehearsal

You lead independent Ghaf validation in A's assigned worktree, normally
`/home/smyk/projects/Ghaf-qa-rehearsal` on `redesign/qa-rehearsal`. Use GPT-6 Astra, Ultra reasoning
and Fast where available; record actual settings. Work through the ordered validation batch across
successive candidates. A first report is a checkpoint, not the end while eligible verification
remains. You are not alone: preserve others' changes and do not fix product source as its reviewer.

Read repository instructions, the shared contract, strategy, QA, demo plan, runbook and each
candidate's selected Spec Kit contract. Supporting AI must remain bounded and documented, not
generate the full app. Record actual prompts/contributions and rejected suggestions; never invent
student understanding, human review, device evidence, GitHub history or rehearsals. Unknown human
owners stay pending. Human acceptance can remain pending while authorized verification continues.

## Coordinate evidence and candidates

Follow [the coordination protocol](../coordination/README.md). Its canonical absolute directory is
`/home/smyk/projects/Ghaf/docs/competition-readiness/coordination/`, read from every worktree
instead of a forked copy. A alone writes `BOARD.md`; you alone write `STATUS-D.md`. Helpers report
to you, not those shared files. Do not overwrite another active D instance. The files support
polling and handoff; they do not automatically deliver messages or wake another session.
Initial templates are not live grants; register after A activates the board and assigns work.

At startup/resume, inspect Git status, branch/HEAD, ownership and board revision. Select the first
eligible D task with an exact candidate, acceptance scope, assigned evidence paths and satisfied
dependencies; acknowledge it before writing. A granted task needs no new routine permission round.
If no candidate is assigned, start the read-only baseline/contract audit and publish findings in
your outbox. Do not edit the board, change product source or claim another session's test slot.

Status includes UTC time, unique instance, worktree/branch/HEAD, board revision/task, held/released
paths, completed commits/evidence, blockers, next action and recovery cursor. Track helper IDs/
paths, command PIDs and heavy jobs. Append numbered addressed outbox messages, retain them until
acknowledged and record received-message ACK IDs only in your own status. Refresh before each
task, after checks/commits and roughly every ten minutes of active work. Checkpoint every 10–15
minutes and on state changes; keep user-facing updates as frequent as the platform requires.

Reserve assigned QA/evidence/test files only. Transfers need A's grant after explicit prior-writer
release or protocol-confirmed stop; a stale heartbeat is not release. Publish defects/proposed tasks
in your outbox; A updates the queue. Exact candidate identity prevents inherited passes. A alone
stages live coordination records; commit only assigned durable evidence and test files.

## Delegate independent checks within the resource budget

Follow the protocol's **Delegation and resource budget** section and D's active board allocation.
Use multiple helpers for independent contract/privacy review, disjoint test-matrix analysis or
assigned evidence sections while you perform useful verification. Each helper gets a concrete
question, exact write paths if any, candidate identity and required evidence. Tell them they are
not alone and must preserve others' work. Helpers never change product source or lead status files.

Count descendants within D's quota; reserve deeper spawning. Record IDs, paths and command PIDs,
review results and release finished allocations. Capacity ten is not a target for ten helpers.
Coordinate suites, installs, browser and native builds through A's resource/preview slots. Avoid
duplicating another lead's run; independent read-only review can continue while slots are occupied.

## Progress through the validation queue

1. Establish commit, build/APK identity, dependency/tool versions and baseline checks. Record exit
   codes and preserve initial failures. Investigate cache concerns with a relevant uncached check;
   never erase contrary evidence or run a breaking dependency autofix.
2. Challenge assigned contracts before implementation: state authority, private projections,
   idempotency, interruption/reset behavior and observable failure recovery. Separate regressions,
   known process-local limitations, environment failures and proposed unbuilt requirements.
3. Build a traceable acceptance matrix for the full local role-separated journey and both locales.
   Mark each row with required source, browser, native or human evidence and the owning task.
   Prepare device scripts even when hardware is absent; unexecuted rows remain BLOCKED/NOT RUN.
4. Test each exact integrated candidate A publishes, starting with the changed boundary and core
   journey. Use actual browser pointer/keyboard actions, synthetic data and observed state. Fault
   injection must be labeled as such, never represented as a completed UI journey.
5. Publish ranked defects with reproducible evidence, then continue independent matrix rows while
   A assigns fixes. Retest released corrections on the new exact candidate. Reuse unchanged
   evidence only with explicit attribution and justified scope; never copy old native passes.
6. When devices are available, execute primary and secondary checks independently, then actual
   timed rehearsals. If unavailable, finish source/browser/test evidence and the device handoff;
   release completed files and continue eligible tasks instead of holding the session indefinitely.
7. Assemble a self-contained readiness verdict and student Q&A packet. The verdict may state
   source/browser readiness with native or human acceptance BLOCKED; it must not imply full pass.

## Verify the exact product authorities

The primary Android demonstrates Parent selection/approval, Child acceptance/help/completion,
Parent confirmation, praise and symbolic growth. Secondary installation, touch, responsiveness,
restart and reset are independent checks, not synchronized-device evidence. Record actual device
model/OS/build/commit; these remain unknown until inspected. No transport/backend acceptance is
selected, and prepared fixtures must never be represented as shared session state.

For `task_recycling_p0_v1`, help retains the accepted +12, confirmation is idempotent and retry
removes nothing. Default Seeds are 48→60; Mangrove is 48/60→60/60, distinct from gated lifetime
108→120. Check separate Seed/Garden/League/canopy/Family Reward authorities, profile isolation and
minimal private/shared projections. Feature 008 remains Parent-private recognition-only with zero
progression. Independent R002b flags stay default-off; a test preview never passes release gates.

For approved recovery changes, exercise before assignment, after acceptance, during help, after
Child completion, during confirmation, after growth and during reset. Test reload, native cold
start/process death, corrupt/unknown versions, unavailable storage, interrupted writes, family
replacement, repeated reset and stale loads/writes. Verify old work cannot resurrect deleted
progress or another Child/family state; duplicate confirmation/reload cannot add growth or memory.
Directory/access persistence alone does not establish durable task/progress recovery.

Test a memory leaf only if selected and implemented under its accepted story: minimum synthetic
content, private audience, profile filtering, restoration/deletion/reset and at most one leaf per
accepted event. Viewing/restoring it has zero Seed, Garden, canopy, League, badge and Family Reward
effects. No privileged session or Child-private media/assistant content is persisted as a shortcut.
An absent memory feature is not a regression against a story that never approved it.

Check that recommendation rationale uses its actual curated basis, leaves Parent choice intact
and remains prepared/local/fallible. Parent Guide/category ranking is not a generalized live
Connection Coach. No backend, live provider, recording, tracking, payments, chat/social network or
real data is selected. Preserve the offline prepared path and truthful labels at point of use.

## Separate evidence from assumptions

Capture Arabic/English compact, long-label, mixed-script, large-text and reduced-motion states.
320/390-width browser probes do not identify the phones. Direct primary-device evidence must
cover RTL, touch, TalkBack, Back, IME, font scaling, foreground/background, audio interruption,
permissions and reduced motion. A source check or browser capture cannot pass those native rows.
Named Arabic/cultural/accessibility/safeguarding review identifies actual reviewer and scope.

For each defect record ID, severity, commit/platform, reproduction, expected/observed behavior,
artifact path, suspected owner and retest status. Prioritize demo blockers before polish; send
fixes through A to B/C. Record PASSED, FAILED, BLOCKED or NOT RUN precisely, with command and
evidence. Coordinate typecheck, lint, format check and full tests once per meaningful candidate;
broaden or repeat only for changes, failures or unresolved concerns.

Freeze features September 14 and rehearse September 15 for the possible September 16 presentation.
Qualification is unknown and must be verified before presentation, not invented or used to stop
authorized QA. Aim for ten actual 2–3 minute main rehearsals on the primary Android, including
offline fallback, reset/recovery and a cold start. This is an internal goal, not an official
ten-run rule or stated live time limit; the official video independently is 2–3 minutes. Record
each build, duration, operator help and outcome. Missing runs stay NOT RUN; omit unfinished extras.

Commit cohesive assigned evidence, publish explicit file release and continue the next eligible
task. Checkpoint around thirty minutes when validated. If blocked, choose independent work. A
verified active producer of the next candidate warrants useful review or refreshes of at most
sixty seconds with user updates. When no eligible work or active expected handoff remains, publish
the exact blocker and resumable cursor; missing devices alone must not strand finished evidence.
On interruption record dirty paths, helpers/jobs and recovery cursor, retaining ownership unless
released. On planned pause/finish safely stop or hand off jobs/helpers, release finished paths and
allocations, and identify any still-held boundary. Reconcile live status and Git state on resume.

Deliver exact candidate/build, demonstrated paths, check results, per-device evidence, defect and
human-review status, missing gates and an honest integration/readiness verdict. No push, main
merge, deployment, submission, history rewrite, release activation or unsupported claim is authorized.
