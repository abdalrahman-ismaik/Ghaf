# Session D — independent QA and rehearsal

You assist a named student with independent evidence for Ghaf's competition candidate. Work in
A's assigned worktree, normally `/home/smyk/projects/Ghaf-qa-rehearsal`, on `redesign/qa-rehearsal`.
Use GPT-6 Astra, Ultra reasoning and Fast where available; report actual settings. Do not spawn
nested agents. You are not alone; preserve other changes and do not fix product source as its
independent reviewer. Reserve only the selected QA/evidence files with A.

Read `docs/competition-readiness/orchestration/shared-contract.md`, current QA, strategy,
`docs/competition-readiness/two-device-demo.md`, runbook and A's selected contract. A named student
selects the bounded QA task, understands the findings and reviews its exact artifacts/diff.
Log actual prompts, contributions and rejected suggestions. Never fabricate human review,
GitHub history, device evidence or rehearsal runs. Test the exact integrated commit A identifies.

Establish the reproducible candidate baseline: commit, APK/build identity, dependency/tool
versions and checks. Coordinate TypeScript, lint, formatting and full tests with A once per
meaningful candidate. Record exit codes; investigate stale cache claims using an uncached check
without erasing the initial evidence. Do not run a breaking dependency autofix. Distinguish an
environment issue, approved process-local limitation, regression and new unbuilt requirement.

Build an acceptance matrix for the complete LOCAL role-separated journey on one primary Android:
Parent selection/approval, Child acceptance/help/completion, Parent confirmation, praise and
symbolic growth. The secondary independently tests installation, responsiveness, touch, restart
and reset; record separate evidence. No cross-device transport or backend acceptance is required,
and independent fixtures must not be represented as synchronization. Exact device models/OS
versions remain unknown until inspected.

For `task_recycling_p0_v1`, verify permitted help keeps the full accepted +12 award, confirmation
is idempotent and retry removes nothing. Default visible Seeds are 48→60; mapped Mangrove is
48/60→60/60. Do not substitute the gated lifetime 108→120 fixture. Check distinct Seed/Garden/
League/canopy/Family Reward authorities, profile isolation and minimal private/shared projections.
Feature 008 connection ideas remain Parent-private, recognition-only and zero-progression.
All independent R002b flags remain default-off.

Exercise actual pointer/keyboard/browser UI operations with synthetic data in Arabic and English.
Capture representative compact, long-label, mixed-script, large-text and reduced-motion states.
320/390-width browser probes do not identify the physical phones or pass native acceptance.
Store injection is permitted only as an explicitly labeled fault test, not a completed UI journey.

Test recovery at each meaningful state: before assignment, after acceptance, during help, after
Child completion, during Parent confirmation, after growth, and during reset. Cover reload,
cold start/process death, corrupt/unknown persisted versions, unavailable storage, interrupted
writes, family replacement, repeated reset and stale pending loads/writes from the previous epoch.
Verify a duplicate confirmation or reload cannot create extra growth or duplicate memory leaves.
An old operation must not resurrect deleted progress or another Child's/family's state.

Current directory/access persistence and task/progress reload loss are different facts. Verify
new persistence only against A's small approved story. A private durable memory leaf currently
has no user-facing implementation; test it only if selected and implemented under its own story.
Require profile/audience filtering, safe restoration and reset, at most one leaf per accepted
event, and no Seed, Garden, canopy, League, badge or Family Reward effects from viewing/restoring
it. No privileged session or Child-private content may be persisted as a shortcut.

Verify recommendations explain their actual curated basis, remain prepared/local/fallible and
leave Parent choice intact. Prepared Parent Guide/category ranking is not a generalized live
Connection Coach. The current competition slice adds no backend, live provider, recording,
tracking, payments, chat/social networking or real data. Keep all prepared offline paths and
honest capability labels; do not pass a live claim from a mock or prepared response.

On the primary phone, record model/OS/build/commit and directly test RTL, touch, TalkBack, Back,
IME, font scaling, foreground/background, audio interruption, permissions and reduced motion.
Record independent secondary installation/touch/restart/reset results. No qualifying device
means BLOCKED or NOT RUN. Named Arabic/cultural/accessibility/safeguarding reviews identify the
actual reviewer and scope; source or browser checks never fill these evidence gaps.

Report defects with ID, severity, exact commit/platform, reproduction, expected/observed behavior,
evidence path, suspected owner and retest result. Rank demo blockers before polish and return
product fixes to B/C through A. Test optional local agenda only after core native/recovery gates
pass and A has selected a small story; study, money, maps, Google integration and chat stay later.

Freeze features September 14 and rehearse September 15 for the qualified September 16 demo.
Aim for **ten actual 2–3 minute main rehearsals on the primary Android**, including local recovery,
the prepared offline path and at least one cold start. This is an internal rehearsal goal, not an
official ten-run rule or separate official live time limit; the official video independently is
2–3 minutes. Log each run's duration, build, operator help and outcome. Missing runs stay NOT RUN.
Do not lengthen the main story to fit an optional feature or imply a proposed memory already works.

Deliver source/build checks, exact demonstrated paths, per-device evidence, student review/Q&A
status, unresolved defects and a self-contained readiness verdict for that build. Release owned
files for A's integration. No push, main merge, deployment, release activation or unsupported claim.
