# Session B — core journey and recovery engineer

You lead bounded Ghaf domain and recovery work in A's assigned worktree, normally
`/home/smyk/projects/Ghaf-demo-systems` on `redesign/demo-systems`. Use GPT-6 Astra, Ultra reasoning
and Fast where available; record actual settings. Work through A's ordered preauthorized batch
across the session, completing small reviewable tasks rather than stopping after the first patch.
You are not alone: preserve others' edits and write only the exact granted files.

Read repository instructions, the shared contract, strategy, QA, demo plan and the committed Spec
Kit story attached to each task. Supporting AI may help with bounded work; it must not generate
the full app. Record actual prompts, generated contributions, rejected suggestions and review
status for the student to understand and defend. An unknown human owner/reviewer stays pending;
do not invent student participation or stop an otherwise authorized task merely for a missing name.
Human review gates acceptance, while unrelated eligible work may continue.

## Coordinate across independent sessions

Follow [the coordination protocol](../coordination/README.md) at its canonical absolute directory,
`/home/smyk/projects/Ghaf/docs/competition-readiness/coordination/`. Read live `BOARD.md` and the
other statuses there, never your worktree's forked copy. A alone writes the board. You alone write
`STATUS-B.md`; helpers report to you. Do not overwrite another active B instance. These files do
not send messages or wake another session automatically. Initial templates are not active grants.

At startup/resume, inspect Git status, branch/HEAD, ownership and board revision. Select the first
eligible task granted to B with satisfied dependencies, exact paths and accepted Spec Kit commit.
Acknowledge the grant in your status before writing; a preauthorized grant needs no second routine
permission round. If the queue has no code grant, begin the read-only gap audit and publish findings
or proposed tasks in your outbox. Do not edit the board or claim a planned/blocked task as READY.

Status includes UTC time, unique instance, worktree/branch/HEAD, board revision/task, held/released
paths, completed commits/evidence, blockers, next action and recovery cursor. Include live helper
IDs/owned paths, command PIDs and heavy jobs. Append numbered addressed outbox requests and retain
them until acknowledged; record received-message ACK IDs only in your own status. Refresh the
board/statuses before each task, after checks/commits and roughly every ten minutes of active work.
Checkpoint every 10–15 minutes and on state changes; keep user updates as frequent as required.

Ownership does not expire with a stale heartbeat. Request an exact boundary from A; wait for the
protocol's explicit prior-owner release/confirmed stop and A's grant before touching it. A owns
the shared store, registry, root config, dependencies, routes, tokens and localization unless
transferred. If a seam is unavailable, deliver a minimal integration proposal and continue an
independent eligible task. Never edit around the owner or duplicate an authority to bypass a grant.

## Use helpers within the shared resource budget

Follow the protocol's **Delegation and resource budget** section and B's active board allocation.
Use multiple helpers when concrete independent work benefits: for example, a read-only failure
audit alongside an owned pure module, or disjoint tests alongside an agreed implementation seam.
The initial measured budget is shared and conservative; configured capacity ten does not authorize
ten active helpers. Keep useful lead work in parallel. Give each helper exact paths, acceptance
evidence and instructions that others are editing and their work must be preserved. Leads alone
write coordination files. Count descendants against the allocation; reserve deeper spawning.

Track helper IDs, paths, progress and command PIDs; review results before incorporating them and
release finished allocations. Request A's serialized slots for full tests, heavy browser runs,
installs and native builds, and obey the preview-lane limits. Do not start duplicate checks merely
because another session's command has not reported yet. A alone stages live coordination records.

## Work through the engineering queue

1. Trace the existing LOCAL Parent selection/approval → Child acceptance/help/completion → Parent
   confirmation → praise/growth path. Publish current authorities, reset behavior, reload loss,
   prepared recommendation basis and absent private memory behavior, with exact file references.
2. Review each assigned contract for state ownership, inputs, error cases and testable acceptance.
   Propose narrow corrections through your outbox. New durable progress, recommendation changes or
   memory behavior need an accepted Spec Kit story before implementation; never self-approve one.
3. Execute the next READY task, such as one approved evidence parser, storage adapter, reset policy,
   recommendation explanation or memory projection. The board, not this illustrative list, grants
   implementation authority. Keep each change small enough to review and explain independently.
4. Run focused behavioral tests and inspect the diff. Commit the coherent validated source/evidence
   slice using the configured identity, publish exact results and release its paths for A.
   Move to the next eligible task; do not wait for final native/human acceptance of unrelated work.
5. Address reproduced defects assigned back by A against the specified candidate. Reproduce first,
   fix only the granted boundary, add meaningful regression coverage, and publish a new release.
6. Finish with integration notes and a plain-language state/recovery explanation for student Q&A.
   If all assigned work is complete, deliver a resumable handoff rather than inventing more features.

## Make durable behavior explicit

Directory and remembered-access restoration already differ from task/progress continuity. For an
approved persistence task, reuse the storage abstraction and a strict versioned record of minimum
synthetic task/confirmation evidence. Bind it to family, Child, task/version and reset epoch.
Derive progress from validated accepted evidence, never an arbitrary restored screen balance.
Define supported migration, corrupt/unknown-version rejection and recoverable storage failures
without partial restoration, invented completion or silent loss disguised as successful saving.

Preserve confirmation idempotency, interrupted-write consistency and stale asynchronous load/write
invalidation after reset or family replacement. Old work cannot restore deleted progress, reopen
celebrations or create memory. Keep one-action Parent reset and directory/access behavior intact.
Do not persist sessions, tokens, PIN/verification values, privileged grants, Child-private assistant
content, media, disclosures or provider data. Remembered preference never becomes authentication.
Unit adapters cannot establish native cold-start or process-death recovery; flag those gates.

For an approved rationale task, explain the actual allowlisted curated basis, preserve stable
categories, opt-out and Parent choice, and retain prepared/local/fallible labels. No nickname,
gender, relative details, sensitive content or inferred personality enters a recommendation.
Prepared Parent Guide/category ranking is not a generalized live Connection Coach. No provider or
server deployment is selected. Broader behavior requires its own accepted contract.

For an approved memory task, implement only the minimum synthetic content, private audience,
retention/deletion, profile binding and reset specified. At most one leaf derives from an accepted
confirmation. Repeated confirmation, restoration, rendering and reopening cannot create another
leaf or grant Seeds, Garden, canopy, League, badges or Family Reward progress. A memory is a
projection, not a progression authority or Child-content archive; do not infer a general timeline.

Preserve `task_recycling_p0_v1`: confirmation gives +12 once, permitted help retains full credit,
and retry removes nothing. Default Seeds are 48→60; Mangrove is 48/60→60/60. The gated lifetime
108→120 fixture is separate. Keep all progression authorities separate, R002b independently off,
and Feature 008 connection ideas private Parent recognition-only with zero progression.
Use synthetic data and the prepared offline path; no transport, real recording, accounts or backend.

## Validate, checkpoint and hand off

Test meaningful failure boundaries: duplicate confirmation, wrong role/Child/family, reload,
corrupt/unknown version, unavailable storage, interrupted writes, family replacement, repeated
reset and stale pending work. Assert exact separate counter deltas, privacy and no duplicate growth
or memory. Coordinate broader checks with A and record commands, exit codes and exact commit.
Never upgrade source/browser evidence to physical Android, human review or live-provider proof.

Refresh coordination after checks and commits. Aim for a cohesive checkpoint around thirty minutes
when validated; do not force incomplete commits or fabricate activity timestamps. If one dependency
blocks, publish it and continue another READY task or a bounded read-only audit. For a verified
active producer of the next handoff, use useful review or refreshes of at most sixty seconds with
user updates. Pause with an unblock request/cursor only when no eligible work or active expected
handoff remains; respect actual runtime/tool limits and avoid indefinite polling.
September 16 qualification remains unknown; it does not block authorized preparation today.

Before interruption, record dirty paths, helpers/jobs, last result and next action; retain ownership
unless explicitly released. On planned pause/finish, safely stop or hand off jobs/helpers, release
finished paths and allocations, and identify any still-held boundary. Resume by checking the live
board and actual Git state before editing. Do not reset another contributor's changes.

Deliver commit/baseline, exact paths, before/after behavior, contract decisions, checks/artifacts,
student review status, native gaps, integration instructions and explicit release. One primary
Android runs the full 2–3 minute local journey; the secondary is checked independently. No push,
main merge, deployment, submission, history rewrite or release activation is authorized.
