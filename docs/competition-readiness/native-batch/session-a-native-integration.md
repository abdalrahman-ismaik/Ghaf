# Session A — product contract, integration and native validation

You are Ghaf's Session A in `/home/smyk/projects/Ghaf`, branch `redesign/ui-experiments`. Execute the
native validation batch, not just its first task. Request GPT-6 Astra, Ultra reasoning and Fast
where available; record actual selection and uncertainty. The user deferred recovery014 until the
current APK/native journey is validated and authorized these new session prompts. No later
recovery approval is implied. B/C/D were stopped when the branches/prompts were prepared.

Read repository instructions in order, then the canonical
`/home/smyk/projects/Ghaf/docs/competition-readiness/native-batch/shared-contract.md`, README,
three worker prompts, entry-onboarding-contract.md, the new a-product-service-review.md,
coordination protocol/BOARD/all statuses, Android guide, D candidate report,
assistance ledger and current demo script. This prompt and shared contract define the bounded
NB1 batch; the old recovery-oriented task queues are historical. Reuse existing evidence.

## A-N01 — activate exact disjoint tasks

Inspect Git/worktrees/status and any live instance/jobs before writing. Preserve a different active
A instance; do not overwrite it. Record a new resume instance or the same actual continuing one.
Verify the prepared B/C/D branches start at52c61fc, runtime-identical to7fff0f3. Never reset a tree
that moved or contains work. The new prompts live in the canonical checkout, not necessarily in
worker snapshots. Read each lead's new status ACK after the human starts it; preparation itself
is not evidence that a session is running.

Activate NB1 on BOARD with exact paths from the shared contract and these initial tasks:

- B-N01: build preflight/script/report. B-N02: isolated host-tool/dependency preparation, contingent
  on measured disk/memory and the heavy slot. B-N03: exact-candidate standalone build after actual
  toolchain/licenses are ready; B-N04: rebuild only an A-published corrected candidate.
- C-N01: reproduce D-R03 and prepare the smallest existing ChildTodayTaskCard-only correction if
  its cause fits that file. C-N02: connected family-experience proposal using the existing research/templates; C-N03:
  native UI checklist/patch acceptance. The later C-N04 grant implements the explicitly requested
  entry/onboarding changes only after A-N05's committed contract.
  A owns QuietButton/shared primitives/tokens/resources until an exact necessary transfer.
- D-N01: read-only device/artifact evidence collector and executable acceptance packet; D-N02:
  independent APK verification and owner-selected installation when supplied; D-N03: actual
  primary/secondary journey and rehearsals, then exact integrated C correction retest if applicable.

Grant one helper per lead (four global), one heavy job and one preview pair subject to no native
build overlap. B receives the initial heavy slot for private dependencies/toolchain/build; grant
C preview only after B explicitly releases the conflicting native-heavy work. No emulator is
selected. Grant B transient prebuild ownership of only `package.json` Android/iOS script fields;
capture the expected generated diff rather than blocking on it. No dependency/lock/app-config
change is authorized. Permit the existing template debug signing identity only for the labeled
internal rehearsal artifact; public distribution and new signing identities remain unapproved. Start neither a second Metro nor duplicate full test pools. Recheck pressure before
install/build and reduce workload if needed. Any host-tool grant must state exact ignored paths,
versions/publisher verification, permitted operations and unresolved operator/license conditions.

## A-N02 — make dependencies concrete and keep work moving

B must remove only its own verified dependency symlink and install the unchanged lockfile privately
before native compilation. Grant that environmental restoration; it changes no app package choice.
Review B's pinned host-tool list against the installed project's actual requirements and approve
only the measured build gap. Downloads/extraction are local; unresolved SDK terms require actual
operator handling, not an invented accepted license. If this blocks B, let B finish its repeatable
scripts/artifact plan and C/D continue independent eligible work. Do not send every worker back to
the already completed broad baseline audit.

Resolve C's reproducible shared-component cause with a minimal exact grant if needed. A may retain
the shared file itself or transfer it explicitly; never allow simultaneous primitive/caller edits.
Do not hide clipping by truncating essential labels, reducing accessibility scaling or changing
business behavior. Native font scaling remains a separate evidence gate. If the reported browser
stress does not reproduce, preserve that result and keep C on its native checklist; no guessed fix.

Provide the real operator/device choice to D when available. Ask only for missing information that
actually gates a concrete prepared step. Missing devices do not prevent read-only tooling, source
repair, build or an honest acceptance plan. Do not fabricate a native pass or treat passing device
restart checks as proof of deferred progress recovery.

## A-N03 — integrate, rebuild and hand off

Collect released B/C/D commits, inspect scope and evidence, preserve authorship, and integrate
locally. Scripts belong in the repository only if safe, understandable and useful to students.
Check shell syntax/help/missing-input/fail-closed behavior without destructive device side effects.
Keep generated native artifacts and sensitive build credentials out of Git.

If C or a justified build correction changes runtime source, publish the exact integrated commit,
run typecheck/lint/format/full suite once, and give B an explicit clean synchronization/rebuild
grant. A may synchronize a clean worker via named commits/branches while preserving history; no
reset/stash. After successful build, verify B's artifact receipt and have D independently retest
that exact hash. Older baseline APKs remain labeled separately. Do not hold a final artifact against
an unpublished moving HEAD, and do not repeatedly rebuild after docs-only commits.

Update the master assistance ledger, Android guide, demo script and student teach-back notes with
what actually ran. Maintain exact source/APK identity, per-device results, pending reviews and
remaining defects. Preserve all eight R002b flags off. Omit recovery/memory/agenda and other
unfinished features from the presentation. The students review/explain exact diffs; Codex is
supporting work, not fabricated student participation or full-app authorship.

## A-N04 — review product value without disguising proposal as delivery

The user explicitly asks whether their ideas were researched and whether app ideas/logic improved.
Answer from evidence: prior research exists, but the last implementation batch mainly stabilized
existing behavior. Review C-N02's concrete product brief for a distinctive family interaction,
reciprocal Parent participation, credible AI value, the user's suggested features and a feasible
small story. Reject generic feature accumulation or a promise of proven developmental outcomes.
Present the proposed before/after journey and recommendation for a real user/student scope choice;
do not self-approve new behavior or stall unrelated APK work. Any selected change needs the
smallest accepted Spec Kit story/contract and exact owned tasks. Recovery014 remains deferred,
and no native result automatically activates it. Update the master strategy only after recording
what was actually selected; preserve previous research and rejected suggestions as history.

## A-N05 — commit the requested demo-entry/onboarding contract

The user now explicitly requests one Parent and two Child synthetic accounts with no authentication,
short attractive onboarding and repaired Arabic narration. Treat this as selected work, not another
permission question or a generic proposal. Read entry-onboarding-contract.md and the completed
access/audio traces in A's report. Use Spec Kit to make the smallest003/005 amendment or additive
feature with a spec, plan, ordered tasks and exact typed contract. Preserve the branch and managed
AGENTS block. Reconcile the six-step/autoplay and ordinary access requirements explicitly; commit
before implementation grants. D reviews failure/privacy cases; actual human acceptance stays pending.

Define isolated demo repositories, exactly three principals, controller-owned sessions, signed-out
selector, current-run handoff without reseeding, zero persisted authority, no custom-family overwrite,
reset/Back and stale-result invalidation. Do not authorize a direct store role mutation, universal
credential, hidden route bypass or duplicated Salem task for Alya. No recovery implementation.
Capture plain-language Q&A explaining why synthetic quick entry is not production authentication.

Give B-N05 an exact pure fixture/access-adapter module and tests after its build work permits; retain
the aggregate store/registry/routes. Give C-N04 exact presentation files, script/candidate assets and
their tests. A owns bilingual resources until transfer, preventing concurrent C translation/store
edits. No new library/provider/account is needed for the access/UI work. Proposed reciprocal family
support remains separate and unselected. A can prepare this contract while baseline APK tools build.

## A-N06 — integrate selected entry and onboarding changes

Integrate the released adapter/presentation modules through existing store/registry/route guards.
Preserve all normal verification/pairing tests. Run meaningful three-principal access, sibling,
authority, unchanged-storage, handoff/reset and stale-async tests. A complete silent onboarding must
work before audio acceptance; new text must never play mismatched old clips. Track Arabic editorial,
asset-rights and real listening review separately from mocked-player lifecycle tests. Publish the
integrated source and full checks once per meaningful candidate, then B rebuilds and D independently
retests that exact artifact. Do not call this work complete from a storyboard or a web-only preview.

Persist through all eligible tasks and expected worker handoffs. Files do not wake an ended A;
if blocked with no active dependency, publish the exact next operator step and resumable cursor.
At handoff, stop/release your jobs/helpers, collect explicit worker releases and coordination-pause
ACKs, commit a coherent snapshot, and report current commit/APK hash, verified before/after changes,
checks, physical evidence, student review and verdict. APK/native may remain BLOCKED with evidence;
no unspecified waiting hours, repeated passing suites or optional product expansion. No push,
main merge, deployment, cloud build upload, submission or release activation is authorized.
