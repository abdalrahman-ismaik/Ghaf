# Session C — onboarding, demo profiles, Arabic voice and product refinement

You own Ghaf's narrow native-UI preparation lane in `/home/smyk/projects/Ghaf-ui-studio` on
`redesign/native-ui-20260912`, prepared clean at `52c61fc`. Runtime source is `7fff0f3`; preserve
the completed botanical direction and earlier branches. Historical 138 files / 1,677 tests and
browser reset evidence do not establish APK/device acceptance. Recovery 014 is deferred until the
current native journey is validated. The user also asks for meaningful product improvement: develop
the concrete proposal below alongside this bounded repair. The user then explicitly selected fast
three-profile demo entry, onboarding redesign and Arabic narration repair. C-N04 implements that
requested work after A's committed contract/exact grant; other feature proposals remain unselected.

Request GPT-6 Astra, Ultra reasoning and Fast; distinguish requested from observable settings.
Apply relevant UI/design-system skills. Record actual prompts, contributions, rejected ideas and
exact-diff human review; unknown reviewers stay pending, never fabricated.

## Start from the active native contract

Read `shared-contract.md` in the canonical prompt directory
`/home/smyk/projects/Ghaf/docs/competition-readiness/native-batch/`, repository instructions and
their required documents, the current build guide and D candidate report. Checkout copies may lack
the new prompts. Read the research-and-product-strategy report and template catalog too. Reuse the
selected visual identity and existing research; distinguish completed code from proposed ideas.
Read `entry-onboarding-contract.md` and `../workstreams/a-product-service-review.md` from the
canonical pack. Reuse the completed service/access/audio findings rather than auditing them again.

Read BOARD and all statuses at
`/home/smyk/projects/Ghaf/docs/competition-readiness/coordination/`. A's resume activation must
precede runtime/helper/resource grants. Inspect actual Git state and role ownership, register a
new unique C instance in `STATUS-C.md`, and acknowledge the active revision/task before editing.
Never overwrite an active lead. A owns BOARD/master documents; C alone writes its status.
Publishing a request does not wake another session.

C's exact runtime boundary is `src/components/r002a/child/ChildTodayTaskCard.tsx`; evidence goes in
`docs/competition-readiness/workstreams/c-native-ui.md`. You also own the proposal-only report
`docs/competition-readiness/workstreams/c-product-refinement.md`. An existing focused card test requires
A's explicit additional grant. QuietButton, shared primitives, tokens, localization, routes and
dependencies remain A-owned unless transferred. Ignored captures and preview processes also need
the current resource/path grant. Do not edit adjacent files because the fix would be convenient.

## C-N01 — reproduce and resolve D-R03 at its actual boundary

Read the preserved raw evidence from
`/home/smyk/projects/Ghaf-qa-rehearsal/output/competition-readiness/d-b2208aa/`:
`ar-large-settled.json`, `ar-large-settled.png` and `ar-large-secondary.png`.
D observed the final word of the Arabic smaller-task secondary label vertically clipped under
injected CSS doubling both font size and line height at 320px. The primary action stayed readable.
This is a P3 browser stress observation, not an established ordinary-scale or Android font-scale
regression. Retain that distinction and preserve earlier failures as evidence.

Inspect the current card and shared control read-only to identify the layout constraint. Request
the shared preview lane through A; it cannot overlap B's native-heavy build. Use one owned Metro
and browser process tree, explicit project root and isolated temporary/cache path. Verify the
actual loaded module/root and candidate before recording results: the earlier mixed-worktree
Metro evidence was invalid even with the correct disk HEAD. Never mutate shared node_modules or
clear another lead's cache. Record root PID, port, temporary path and loaded-source proof.

Reproduce ordinary Arabic/English at 320 and 390 widths, then the exact settled CSS stress state.
Capture text/button geometry and the visible final word; width equality does not prove readability.
Separate harness/transition failures from defects. If unreproduced, document compared conditions.

Make the smallest correction within the existing Feature 003 visual scope when the card owns the
cause. Preserve props, callbacks, selection/disabled behavior, complete bilingual labels, permitted
help and the full accepted award. Retain Alexandria/Readex, logical RTL, accessible names and touch
targets. Do not disable font scaling, truncate meaningful copy, shrink text to hide the symptom,
hard-code translated sibling text or replace the shared control with a competing implementation.

If the cause belongs to QuietButton or another shared primitive, publish the exact diagnosis,
minimal proposed change and requested file transfer to A. Do not patch around its owner. Continue
C-N02 product refinement while A addresses that dependency. No new token, translation authority, UI library or business
behavior is authorized by the C-N01 initial grant. C-N04 has its own committed-contract gate.

Validate a resulting candidate at normal and enlarged Arabic/English 320/390 states, including
the complete secondary label, main action, focus, selection, wrapping and reduced-motion state.
Run proportional checks on granted files and meaningful interaction tests where assigned; do not
rerun every historical audit or add tests that merely mirror styles. Publish a coherent commit,
paired before/after captures, exact candidate identity and explicit path release. A integrates it;
D independently retests the integrated source, and B builds the candidate A selects.

## C-N02 — make the family proposition substantively stronger

The user can still see the old ideas/logic and asks what happened to the suggested calendar,
study/custom goals, money literacy, maps and chat. Do not answer with another list of technical
fixes or imply those features shipped. Produce `c-product-refinement.md` while native dependencies
are in flight. Use the existing research report and template catalog; don't repeat archive
extraction, a generic competitor catalog or three unrelated visual themes.

Evaluate each suggested idea against the family-bonds theme, a 2–3 minute demonstration, offline
feasibility, student explainability, privacy and current product boundaries. For each, state what
exists, the concrete proposed behavior, the family interaction it creates, supporting evidence
and limitations, implementation dependencies and keep/propose-later/reject rationale. Verify any
new scientific claim against primary research or official guidance; distinguish an evidence-based
design principle from proof Ghaf improves cognition or family wellbeing. No fabricated interviews.

Recommend one connected, small product story rather than disconnected modules. A useful hypothesis
to assess is a reciprocal family agreement: the Child chooses an attainable goal, the Parent
commits a specific support action, help remains welcome, and both acknowledge what they did.
Compare that against the current Child-task/Parent-approval loop and explain exactly what changes.
This is a hypothesis, not an approved feature name or product decision. Consider a private memory
leaf only as a later dependent part; recovery/durability is deferred and no memory is built now.
A study example must support strategy/practice rather than paying for marks; education cannot
silently become Family Reward-eligible. Money practice is not custody, maps are not surveillance,
and communication does not require an unrestricted chat platform.

Deliver one proposed Parent/Child storyboard, before/after interaction table, honest AI role and
rationale, expected state transitions, likely file seams, invariants, failure/privacy cases,
rough relative effort/dependencies and a recommended smallest acceptance story. Name selected
reference-template ideas with their provenance; do not copy unapproved assets. Include a clear
student/user decision and review section marked pending. No new spec is accepted, app behavior
implemented, points granted, flags enabled or outcome proved by this document. A reviews the
concrete brief with the user while APK work continues. New implementation requires its own accepted
contract and exact grant; native validation alone does not grant it.

## C-N03 — turn the fix into actionable native checks

Prepare a concise phone checklist in your report while the APK is being produced. Name the route,
synthetic setup, ordinary/enlarged text state, complete expected labels, touch/focus/Back checks,
RTL/mixed-script expectations, reduced-motion behavior and the exact screenshots to collect.
Record actual device/model/OS/font settings only when observed. CSS 200% is not native font scale.

When A supplies an APK/device task, compare the installed candidate with the browser result under
the device owner's approved settings. Coordinate with D instead of duplicating its device session.
Do not alter global accessibility settings without that owner's agreement. Missing hardware leaves
native rows BLOCKED/NOT RUN; it does not prevent completing the source fix, checklist and handoff.
Keep ordinary browser, CSS-stress and native evidence in separate rows with their own hashes.

## C-N04 — deliver the requested entry/onboarding experience

This is the primary new design workstream. While A-N05 prepares the formal contract, create the
concrete storyboard/script in your granted report; once A commits and grants exact component/test
paths, implement it. Do not stop at a moodboard or another broad audit. Entry must offer exactly
one synthetic Parent, Salem and Alya without OTP/PIN/pairing/setup. It is a signed-out demo selector;
existing authenticated routes remain separate. A supplies callbacks and authoritative state;
your components never mint sessions, change roles or reseed progress.

Use the Impeccable onboarding workflow and existing Expo design-system conventions. Preserve
Tamagui, Alexandria/Readex and the approved botanical assets, but give onboarding a deliberate,
modern composition and clear family story. The brief proposes three skippable moments instead of
the current six; only the amended contract authorizes that behavior. Show demo entry immediately,
keep language/skip controls obvious and make the complete experience usable without narration.
Implement actual CTA/disabled/loading/error/Back behavior, accessible reading order, generous touch
targets, long Arabic labels, image failure and reduced motion. Don't add dependencies or copied
template assets; use the existing catalog for composition references with attribution.

Arabic narration is bundled MP3, not runtime TTS. First prepare concise MSA scripts and equivalent
English, retain required safety/AI/symbolic-growth meaning and obtain named Arabic review before
acceptance. Revoice reviewed text with an available approved method; if no suitable method/reviewer
is available, complete the visual work and exact recording packet with audio acceptance BLOCKED.
Do not replace audio quality review with file-size or checksum tests, and don't present an English
voice or old mismatched Arabic clip as a repaired narrator. No paid service/account/voice cloning
or real Child recording is selected. Track clip provenance and rejected takes honestly.

Implement the contract's optional play/stop/replay behavior, cancellation on navigation/locale/
background, screen-reader priority and text-only fallback. Locale resources remain A-owned until
an exact transfer; coordinate text/audio changes as one candidate to preserve transcript parity.
Compare actual Arabic/English screens in one bounded visual pass and confirm corrections once;
record native large-text/TalkBack/Back/audio results separately with D. Release coherent component,
script and accepted asset slices; A integrates and B rebuilds the exact candidate.

Task priority once eligible: C-N04 requested onboarding/entry before extending the optional C-N02
product proposal. C-N01 remains a small existing defect task, not the entire redesign mission.

## Sustain useful work and release cleanly

After activation C has one helper in the global four-helper budget including A. Delegate independent
source/evidence review alongside useful lead work; reserve exact paths, preserve others' edits and
count descendants. Unreserved deeper spawning and helper coordination writes are forbidden. Track
IDs/PIDs and releases; obey A's heavy/preview slots. Capacity ten does not authorize forty helpers.

Refresh all live statuses before tasks, after checks/commits and about every ten minutes. Publish
UTC, unique instance, branch/HEAD, board/task, held/released paths, helpers/PIDs, evidence, blockers
and recovery cursor. Keep numbered outbox requests until acknowledged and ACK IDs in your own
status. Checkpoint every 10–15 minutes/state change; user-facing updates remain more frequent.
Make cohesive validated commits around thirty minutes when appropriate; never fabricate activity.

Continue eligible work after each handoff. While a verified active producer prepares your next
dependency, use useful review or bounded refreshes of at most sixty seconds. When no eligible work
or active expected handoff remains, deliver the precise blocker and resume step. Release finished
files and jobs explicitly; interrupted dirty work stays held unless honestly released. A stale
heartbeat is not a transfer. Deliver exact diffs/captures/checks, student-review status and native
gaps. No push, main merge, deployment, submission, unselected feature expansion or release flags.
