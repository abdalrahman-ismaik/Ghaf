# Session D — independent APK and physical-device acceptance

You own independent native evidence in `/home/smyk/projects/Ghaf-qa-rehearsal` on
`redesign/native-qa-20260912`, prepared clean at `52c61fc`. It contains runtime `7fff0f3` and
historical 138 files / 1,677 tests. D-R02 is closed only for its recorded browser reset subset;
D-R03 remains Arabic CSS-stress clipping. No APK, identified phones or actual rehearsal passes
exist yet. Recovery 014 is deferred, so existing process-local progress loss must remain explicit.

Request GPT-6 Astra, Ultra reasoning and Fast; distinguish requested from observable settings.
Record actual AI prompts, contributions and rejected ideas. Human review stays pending until
observed; scripts cannot establish participation or timed human rehearsals.

## Register against A's active batch

Read the canonical prompt pack at
`/home/smyk/projects/Ghaf/docs/competition-readiness/native-batch/`, especially `shared-contract.md`,
then repository-required documents, build guide, D candidate report and device handoff. Worktree
copies may lack this pack. Reuse completed audits without promoting browser evidence to native.

Read BOARD and all statuses at
`/home/smyk/projects/Ghaf/docs/competition-readiness/coordination/`. A must resume and activate
runtime/helper/resource grants first. Inspect Git/branch/HEAD and prior role ownership, register
a unique D instance in `STATUS-D.md`, and acknowledge the task/revision before writing. Do not
overwrite an active instance. D alone writes its status; A owns BOARD/master records. These files
coordinate handoffs but cannot wake stopped sessions.

Your initial exact scope is `scripts/native/collect-device-evidence.sh` and
`docs/competition-readiness/workstreams/d-native-acceptance.md`. Product source and shared tooling
remain read-only. Additional ignored artifact destinations need A's grant. Never fix product code
while acting as its independent reviewer or edit another lead's report/status.

## D-N01 — build a safe evidence collector now

Create a read-only collector requiring explicit APK path and device serial; never select the first
phone or infer the artifact. Provide help/preflight and actionable missing-prerequisite errors.
Validate APK/hash, tools, exact transport and target package before inspection. Offline, unauthorized
or ambiguous transport blocks device work; do not reconnect/reset automatically.

Collect allowlisted UTC/exit codes, APK SHA-256, package/version/signing, model/OS/ABI and target-
package identity. Bind results to commands, source/build candidate and absolute artifacts. Missing
tools/values stay BLOCKED/NOT RUN. Preserve pipeline failures; empty output or command success alone
does not establish acceptance.

Default behavior must not install/uninstall, clear data, reset, force-stop, toggle settings, accept
permissions, record media/screenshots or collect broad properties/logcat/personal data. Additional
capture needs explicit owner approval and narrow synthetic-app scope. Keep serials/raw artifacts
in the local evidence boundary; exclude unrelated personal information.

Check syntax, help and safe argument/prerequisite failures without claiming device execution.
Document invocation, output schema, failures and next steps. Commit/release the collector/checklist
even without a phone; a known empty device list is not this phase's whole deliverable.

## D-N02 — independently inspect the real APK and installation

Wait for B's actual artifact and A's published candidate/hash. Verify the received SHA-256, package,
version, ABIs, certificate/signature, merged permissions, backup setting and embedded JS/assets
against the approved configuration. Record what D checked independently versus B's attributed
build results. A JS export, AAB, unsigned file or Metro-dependent build does not satisfy the APK gate.
A debug-keystore rehearsal artifact is not approved public-distribution signing.

Install only after the device owner authorizes the chosen observed serial and exact APK. Target
that serial explicitly. Distinguish fresh install from state-preserving update. Stop on signature,
package, ABI or downgrade mismatch; never uninstall, clear data or change signing to bypass it.
Record command/exit and installed identity. Permission/USB authorization requires the actual owner;
missing access is not an automatic rejection of the application.

Coordinate slots with A; native builds cannot overlap resident browser/Metro. Repeat full tests only
for changed source/concerns. Independently retest C's integrated fix, separating CSS-stress/native
verdicts. Verify the loaded source root for any browser preview.

## D-N03 — execute the primary and secondary journeys

The primary Android must run the complete 2–3 minute local role-separated journey: Arabic-first
entry, Parent selection/approval, Child acceptance/help/completion, Parent confirmation, praise and
symbolic growth. Test English equivalence and prepared assistance too. Confirm Metro-independent
cold launch/offline behavior with the owner and A; never stop another lead's process yourself.

Preserve `task_recycling_p0_v1`: +12 once, full credit with permitted help, no loss on retry, default
Seeds 48→60 and Mangrove 48/60→60/60. Keep separate counters/private projections and default-off
flags. The gated lifetime 108→120 fixture is distinct. Do not claim production accounts, sync,
live inference/media, measured environmental impact or newly durable recovery.

Directly inspect RTL, touch, TalkBack names/order, keyboard/IME, Back, font scaling, safe areas,
reduced motion, foreground/background, audio interruption and permissions using prepared synthetic
inputs. Split restart/reset evidence by mode: in the ordinary prototype, validated remembered
access markers may persist while task/progress resets; in the new isolated demo mode, restart must
open the signed-out picker and never restore a selected principal/session. Neither behavior is
a recovery014 pass or implementation grant. Capture failures and route/counter evidence without
real Child media or broad personal logs.

Check the same APK on the secondary phone independently: install, layout, touch, responsiveness,
restart/reset and applicable accessibility rows. No synchronization is expected. Aim for ten actual
human-operated primary rehearsals, recording build, operator, duration, assistance and outcome.
Automation is not a human run; absent runs remain NOT RUN. Qualification is unknown until verified.

## D-N04 — independently validate demo entry, onboarding and Arabic narration

Read canonical `entry-onboarding-contract.md` and A's product/service report. Review A-N05's
proposed typed contract for failure/privacy cases before implementation; do not repeat the completed
broad audit. The user selected three synthetic no-auth profiles and improved onboarding/narration;
this is distinct from deferred recovery014. A must publish exact accepted contract/source/paths
before you write any new tests outside your initial collector/report boundary.

Test Parent, Salem and Alya entry from signed-out demo mode with no authentication/setup. Verify
one selected controller session, correct routes/actions, denied other-role and sibling actions,
Back isolation, rapid double taps, failed-entry cleanup, stale completion after reset, sign-out/
re-entry without reseeding current progress and a repeatable exact Arabic reset. Seed a separate
ordinary local family and verify its storage is unchanged by the entire demo run. Process restart
must not restore privileged authority or claim unimplemented progress recovery. Alya must not
receive Salem's sole executable task through an account-selector shortcut.

Onboarding must reach demo entry immediately, remain skippable, work without every audio/image,
show equivalent AR/EN wording and respect native safe areas, large text, TalkBack and reduced motion.
Measure cold start and settled tap-to-home separately on actual named hardware; no guessed latency.
Test optional audio start/stop/replay and navigation/locale/background cancellation. Arrange actual
Arabic speaker listening to exact final clips for grammar, pronunciation, clarity, pace, transcript
parity and Ghaf pronunciation. Record reviewer/date/artifact and defects; do not invent human
approval or claim to have heard clips from source metadata. Missing reviewer leaves voice quality
BLOCKED while other acceptance work proceeds. Independently confirm A's Parent-approved instruction
repair on chosen and active task screens in both languages; no changed award/safety behavior.

## Maintain useful progress and an honest verdict

After activation D has one helper in the global four-helper budget including A. Delegate independent
script/evidence review alongside useful lead work; preserve others' edits, reserve paths/descendants,
forbid unreserved recursive spawning and helper coordination writes. Track IDs/PIDs and grants. Capacity ten
does not authorize forty helpers or parallel heavy jobs.

Refresh live coordination before tasks, after checks/commits and about every ten minutes. Publish
UTC, instance, branch/HEAD, task/revision, held/released paths, helpers/jobs, evidence, blockers and
recovery cursor. Retain numbered outbox requests until ACK; acknowledge received IDs in your own
status. Checkpoint every 10–15 minutes/state change, with more frequent user updates and coherent
validated commits around thirty minutes when appropriate. Never fabricate timestamps or evidence.

Continue eligible tasks while another row is blocked. For a verified active artifact/fix producer,
review usefully or refresh in intervals no longer than sixty seconds until handoff. When no eligible
work or active expected handoff remains, publish the exact external gate and resumable checklist.
Release finished files/helpers/jobs; preserve interrupted dirty work and truthful ownership.
Deliver PASSED/FAILED/BLOCKED/NOT RUN per artifact/platform, reproducible defects, student-review
status and explicit integration release. No push, main merge, deployment, submission or release flags.
