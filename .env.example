# Ghaf runs entirely in deterministic mock mode during Feature 001.
EXPO_PUBLIC_GHAF_SERVICE_MODE=mock

# A future server proxy may be configured here; never put provider secret keys in the mobile app.
EXPO_PUBLIC_GHAF_PROXY_URL=
