# Ghaf defaults to deterministic mock mode.
EXPO_PUBLIC_GHAF_SERVICE_MODE=mock

# Optional AI-only extension. Point this at the deployed Ghaf Worker; never put provider API keys
# or Cloudflare credentials in an EXPO_PUBLIC variable.
EXPO_PUBLIC_GHAF_AI_GATEWAY_URL=
